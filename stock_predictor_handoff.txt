Stock Predictor Pro — Project Handoff Brief
===========================================
You are taking over an active production project. Read this fully before touching anything.


WHAT THIS IS
------------
A stock prediction website called Stock Predictor Pro. It uses a machine learning model
(HistGradientBoosting) trained on technical indicators + options data to predict whether
a stock will move +-5% within 10 trading days. There's also a market scanner, a daily
Telegram-based analyst report, and a FRED macro dashboard.

Live URL: stock-predictor.online
GitHub:   doctor-magic/Stock-predictor (branch: main)


SERVER ACCESS
-------------
SSH:
  ssh -i ~/.ssh/gcp_stock_rsa elimaoz99@35.239.74.178

SCP (copy file to server):
  scp -i ~/.ssh/gcp_stock_rsa <local_path> elimaoz99@35.239.74.178:<remote_path>

- GCP VM, Ubuntu, IP: 35.239.74.178
  MACHINE TYPE: e2-small (2 vCPU shared, 2 GB RAM) since Aug 23 2026 — downsized from
  e2-medium. It has a 4 GB /swapfile (in /etc/fstab) and MUST KEEP IT: pre_scan.py peaked
  at 2.84 GB on Aug 19, which is over the 2 GB of RAM. Stress-tested to 2,900 MB on
  Aug 23 — survived, 0 OOM, swap took only 220 MB. Never run e2-small without the swapfile.
  EXTERNAL IP IS STATIC (reserved as "stock-app-ip", Aug 23 2026). It used to be ephemeral,
  which meant stopping the VM would have changed it — and 7 files hardcode 35.239.74.178,
  including pull_backup.py (both copies) and deploy.sh. Always reserve before stopping.
  TAILSCALE: installed Aug 23 2026, tailnet IP 100.84.182.68, tailscaled enabled at boot,
  ~39 MB RSS. Gives phone/laptop access to the app without going through the public
  internet: http://100.84.182.68:8000
- SSH user: elimaoz99 (NOT elima_oz, NOT root)
- SSH key: ~/.ssh/gcp_stock_rsa
- Sudo: elimaoz99 has passwordless sudo ONLY for: "systemctl restart stock-app.service",
  /bin/chown, /usr/bin/chown, /usr/bin/chattr (verified via `sudo -n -l` Jun 21 2026; the old
  "restart only" note here was wrong — chown was used Jul 10 to fix the root-owned FK db).
  GENERAL SUDO — how to get it when you need it (Aug 23 2026): elimaoz99 is in NEITHER the
  `sudo` nor the `google-sudoers` group. Use the GCP Console "SSH in browser" button
  (Compute Engine -> VM instances -> SSH); that session is auto-granted passwordless sudo.
  Do NOT try `usermod -aG google-sudoers elimaoz99` — it appears to work and then the GCE
  guest agent silently reverts it within minutes (observed Aug 23). A file in
  /etc/sudoers.d/ would survive, but that undoes the Aug 5 hardening — ask the user first.


CRITICAL: TWO APP DIRECTORIES EXIST — ONLY ONE IS ACTIVE
----------------------------------------------------------
  /home/elimaoz99/stock_predictor/   <-- ACTIVE, edit this
  /home/elimaoz99/stock_app/         <-- do NOT edit, but do NOT delete either

The systemd service stock-app.service runs stock_predictor, not stock_app.
This has caused bugs before when the wrong file was edited.
Always confirm the active path before editing.

BUT stock_app IS LOAD-BEARING (established Aug 23 2026). It is not dead weight and
deleting it breaks production:
  - generate_report.py line 10 loads /home/elimaoz99/stock_app/.env  <-- the ACTIVE
    15:00 Mon-Fri cron dies without it.
  - 138 archived stock_recommendations_*.txt reports live there; the 15:00 cron copies
    a fresh one in daily.
  - Its 484 MB venv is what runs the two fetch_24h.py crons (12:00 and 18:00), which
    still work — the telegram session file is touched on every run.
The live API does NOT serve reports from there: _report_dir() returns dirname(api.py),
i.e. stock_predictor. So the stock_recommendations_22_05_2026_*.txt files with the
frozen May date are confusing duplicates, not a UI bug.
DISABLED Aug 23 2026: the `tg_scraper.py` cron (05 12,18) — it had been failing on every
single run with ModuleNotFoundError: matplotlib. Crontab backup:
/home/elimaoz99/crontab.backup-20260823-072946.txt


SYSTEMD SERVICE
---------------
Restart (after deploying backend changes):
  ssh -i ~/.ssh/gcp_stock_rsa elimaoz99@35.239.74.178 "sudo systemctl restart stock-app.service"

Live logs:
  ssh -i ~/.ssh/gcp_stock_rsa elimaoz99@35.239.74.178 "sudo journalctl -u stock-app.service -f"

Check status:
  ssh -i ~/.ssh/gcp_stock_rsa elimaoz99@35.239.74.178 "sudo systemctl is-active stock-app"

The service runs: python3 -m uvicorn api:app --host 0.0.0.0 --port 8000
Working directory: /home/elimaoz99/stock_predictor/

NEVER STOP THIS SERVICE TO "TURN OFF THE WEBSITE" (established Aug 23 2026).
warm_volume_cache.sh COLLECTS DATA THROUGH THE API — it runs
  curl --user "$CREDS" "http://localhost:8000/api/volume-leaders?force=true"
every 25 min, and that call is how gainers/reversion rows reach setup_log. Stopping
uvicorn silently reopens the collection gap that was fixed Jul 26 2026 and starves R1's
B2_RVOL_NO_ML arm. If the goal is "take the site off the internet", close the ports at
the firewall or use Tailscale — do not stop the service.
That warm cron has a market-hours gate (added Jul 5 2026): it only really runs
09:00-16:59 ET on NYSE trading days = 16:00-24:00 Israel time, weekends/holidays skipped.
Server TZ is Asia/Jerusalem, so ALL crontab times are Israel time, not UTC.


BACKEND (api.py)
----------------
Local copy:  ~/Desktop/Stock-predictor/api.py
Server copy: /home/elimaoz99/stock_predictor/api.py

Deploy backend change (ALWAYS deploy all 3 together — api.py imports scanners + db):
  scp -i ~/.ssh/gcp_stock_rsa \
    ~/Desktop/Stock-predictor/api.py \
    ~/Desktop/Stock-predictor/scanners.py \
    ~/Desktop/Stock-predictor/db.py \
    elimaoz99@35.239.74.178:/home/elimaoz99/stock_predictor/
  # then restart service
  # WARNING: deploying api.py alone causes ImportError on startup → site down

Endpoints:
  GET  /api/predict/{ticker}          single stock prediction
  POST /api/scan                      market scan (async, background thread)
  GET  /api/scan/progress/{task_id}   poll scan progress
  GET  /api/recommendations           daily Telegram report text files
  GET  /api/macro                     MacroPulse strip (VIX, 10Y, SPY, regime); 1h cache
  GET  /api/macro-dashboard           FRED 11-indicator dashboard; 6h cache; needs FRED_API_KEY
  POST /api/earnings-calendar         upcoming earnings dates for a list of symbols (see below)

IMPORTANT: api.py self-loads api_data.env at startup (reads the file directly via Python,
not via systemd EnvironmentFile). Adding a key to api_data.env takes effect on next restart
— no service unit changes needed.

Environment file: /home/elimaoz99/stock_predictor/api_data.env
Contains: TG_API_ID, TG_API_HASH, TG_PHONE, GEMINI_KEY, FRED_API_KEY, TELEGRAM_BOT_TOKEN,
          TELEGRAM_CHAT_ID, BASIC_AUTH_USERS, ENABLE_AUTH
          (BASIC_AUTH_USERS + ENABLE_AUTH added May 2026 — their absence from the file loaded
           first caused the silent Jun 6–14 401 outage; see 'BUG 2/BUG 3' at end. live_tracker.py
           loads api_data.env FIRST, then .env, via os.environ.setdefault().)

Other backend modules:
  scanners.py    ALL scanner helpers: compute_verdict, compute_momentum, classify_regime,
                 detect_falling_wedge, get_intraday_signals, get_market_context,
                 get_tod_rvol_cached, get_overhead_supply, get_vaccel + module-level caches.
                 No imports from api.py — do not add any (circular import).
  db.py          SQLite logic: scanner_cache.db (original) + FK log (falling_knife_log.db)
                 + setup log (setup_log.db). All connections: timeout=30. WAL mode on the two
                 high-write logs ONLY (setup_log.db + falling_knife_log.db); cache DBs do not use WAL.
  core_logic.py  ML model training and prediction logic
  models.py      Pydantic schemas


FRONTEND (App.jsx)
------------------
Source:        ~/Desktop/Stock-predictor/frontend/src/App.jsx (also on GitHub)
Compiled dist: /home/elimaoz99/stock_predictor/frontend/dist/

9 tabs:
  1. Single stock prediction
  2. Market scanner with MacroPulse strip at top + ALMOST BUY section (see below)
  3. Daily Telegram report (rendered as markdown via react-markdown)
  4. FRED macro dashboard (11 indicators with sparklines)
  5. Macro score (bull_score gauge)
  6. Leumi Options — employee stock options simulator (see below)
  7. Volume Leaders — most active US stocks by volume + ML signal
  8. Wedge Scan — falling wedge pattern results from overnight pre_scan (see below)
  9. Reversion Hunter — Yahoo day_losers ≥5% drop, mean-reversion ML engine (see below)

Google Analytics: Tag G-5KHC440K09 added to frontend/index.html (May 1 2026).
  gtag script is in the <head> of index.html — do NOT remove it on future builds.

Deploy frontend changes (from ~/Desktop/Stock-predictor/frontend/):
  npm run build
  scp -r -i ~/.ssh/gcp_stock_rsa dist/ \
    elimaoz99@35.239.74.178:/home/elimaoz99/stock_predictor/
  # copy FULL dist folder — not just index.html (Vite hashes filenames each build):
  ssh -i ~/.ssh/gcp_stock_rsa elimaoz99@35.239.74.178 \
    "cp -r /home/elimaoz99/stock_predictor/dist/. /home/elimaoz99/stock_predictor/frontend/dist/"
  ssh -i ~/.ssh/gcp_stock_rsa elimaoz99@35.239.74.178 "sudo systemctl restart stock-app.service"

NOTE: Two dist folders exist on server:
  /home/elimaoz99/stock_predictor/dist/          — uploaded via scp
  /home/elimaoz99/stock_predictor/frontend/dist/ — ACTIVE (served by FastAPI)
  Always cp -r the full dist/ to frontend/dist/ after uploading (not just index.html).

Frontend uses RELATIVE URLs for API calls (fetch('/api/...')).
NEVER hardcode http://localhost:8000.
Predict fetch uses { cache: 'no-store' } to bypass browser HTTP cache (added May 2 2026).

UI rules:
  - Tailwind dark theme
  - All new components need sm: breakpoints for mobile
  - Do NOT put JSX inside module-level object literals — causes Vite/Rolldown parse error
  - Review tab uses react-markdown with CSS class "review-md" (styles in index.css)


FRED MACRO DASHBOARD
--------------------
/api/macro-dashboard fetches 11 FRED series in parallel via ThreadPoolExecutor(max_workers=6).

FRED API call format (FIXED May 4 2026 — do not revert):
  Monthly series (most indicators):
    ?series_id=CPIAUCSL&limit=26&sort_order=desc&api_key=<KEY>&file_type=json
    (NO frequency or aggregation_method — adding them causes HTTP 500 on FRED)

  Daily series only (DGS10, DGS2 — marked "daily": True in FRED_INDICATOR_META):
    ?series_id=DGS10&frequency=m&aggregation_method=avg&limit=26&sort_order=desc&api_key=<KEY>&file_type=json

  _fred_obs(series_id, limit, daily=False) — daily flag controls which format is used.
  NEVER use aggregation_method=eop — FRED returns 500 for it on CPILFESL, FEDFUNDS, UMCSENT, CFNAI.

Uses urllib.request (stdlib only) — no external library needed.

11 indicators:
  CPI        CPIAUCSL   %    yoy    good=down    monthly (no freq override)
  CORE_CPI   CPILFESL   %    yoy    good=down    monthly (no freq override)
  PCE        PCEPI      %    yoy    good=down    monthly (no freq override)
  UNRATE     UNRATE     %    raw    good=down    monthly (no freq override)
  PAYROLLS   PAYEMS     K    mom    good=up      monthly (no freq override)
  10Y        DGS10      %    raw    good=neutral  DAILY → daily=True, freq=m,avg
  2Y         DGS2       %    raw    good=neutral  DAILY → daily=True, freq=m,avg
  M2         M2SL       %    yoy    good=neutral  monthly (no freq override)
  FED_FUNDS  FEDFUNDS   %    raw    good=neutral  monthly (no freq override)
  CFNAI      CFNAI      idx  raw    good=up      monthly (no freq override)
  SENTIMENT  UMCSENT    idx  raw    good=up      monthly (no freq override)

Cache gotcha (FIXED Jun 1 2026 — disk cache added):
  In-memory cache (_macro_dash_cache) is lost on every service restart.
  Disk cache: /home/elimaoz99/stock_predictor/fred_cache.json
    - Loaded into _macro_dash_cache at module startup via _load_fred_disk_cache()
    - Saved after every successful full fetch (≥4 valid indicators) via _save_fred_disk_cache()
    - Survives service restarts — no FRED calls needed until TTL expires after restart
    - DO NOT DELETE fred_cache.json on the server — this breaks the startup warmup
  Fetch is SEQUENTIAL with time.sleep(0.5) between each series (ThreadPoolExecutor was REMOVED).
    Why: FRED rate-limits at ~2 concurrent requests → HTTP 429 → nulls cached 6h silently.
  Stale-cache fallback: if valid < 4 indicators returned by fetch → serve existing cache instead
    of overwriting with nulls.


GIT WORKFLOW
------------
cd ~/Desktop/Stock-predictor
git add <files>
git commit -m "message"
git push

~/Desktop/Stock-predictor IS the git repo (branch: main).
After changes, git push keeps GitHub in sync, but the running server needs
files scp'd manually AND a service restart.


IN-MEMORY CACHES (reset on service restart)
--------------------------------------------
api.py caches:
  _macro_cache         1h    /api/macro endpoint
  _macro_dash_cache    6h    /api/macro-dashboard endpoint
  _macro_score_cache   2h    /api/macro-score endpoint

core_logic.py caches (cachetools TTLCache — also reset on restart):
  fetch_stock_data()         15min  per-ticker OHLCV (TTLCache maxsize=100, ttl=900)
  fetch_options_features()   15min  per-ticker options chain
  fetch_macro_timeseries()   1h     FRED macro series (dgs10, t10y2y, vix)
  load_sp500()               15min  ticker list from Wikipedia  (@cached(_sp500_cache))
  load_nasdaq100()           15min  ticker list from Wikipedia  (@cached(_nasdaq_cache))

CRITICAL: load_sp500 and load_nasdaq100 MUST use SEPARATE TTLCache instances (_sp500_cache,
_nasdaq_cache). cachetools @cached uses function args as cache key — NOT function identity.
Both functions take no args → key=(). If they share a cache object, whichever runs first
(sp500) poisons the other's result. Bug was live until May 5 2026 — pre_scan was scanning
SP500 stocks for both markets, producing identical Telegram reports.

Scanner results cache IS persistent (SQLite scanner_cache.db).
No Redis — a service restart clears ALL in-memory caches.


PRE-SCAN CRON (05:00 daily)
----------------------------
Script: /home/elimaoz99/stock_predictor/pre_scan.py
Scans sp500, nasdaq100, tase — stores results in SQLite for instant scanner results.
Log: tail /home/elimaoz99/stock_predictor/pre_scan.log

Cron entry (updated May 1 2026):
  0 5 * * * cd /home/elimaoz99/stock_predictor && venv/bin/python3 -u pre_scan.py >> pre_scan.log 2>&1
  (-u = unbuffered, so output appears in log in real-time)

IMPORTANT: pre_scan.py loads api_data.env at startup (first few lines).
Without this, FRED_API_KEY is empty in cron environment → dgs10/t10y2y all NaN
→ dropna removes all rows → shape=(0,20) error → 0 results for all markets.

On completion, pre_scan.py sends a Telegram alert:
  ✅ success: "[Stock Predictor] ✅ סריקה הושלמה בהצלחה\n  ✅ SP500: N סיגנלים (BUY:x SELL:y)\n  ..."
  ⚠️ failure: "[Stock Predictor] ⚠️ סריקה הושלמה עם כשלים\n  ❌ SP500: שגיאה — ...\n  ..."

Debug scripts on server:
  venv/bin/python3 debug_fred.py        — tests FRED fetch + builds features for AAPL
  venv/bin/python3 check_scan_results.py — shows current DB results per market

Run pre_scan manually:
  ssh -i ~/.ssh/gcp_stock_rsa elimaoz99@35.239.74.178 \
    "cd /home/elimaoz99/stock_predictor && nohup venv/bin/python3 -u pre_scan.py >> pre_scan.log 2>&1 &"


DAILY TELEGRAM REPORT PIPELINE
--------------------------------
Scripts location: /home/elimaoz99/stock_predictor/
Python:           /home/elimaoz99/stock_predictor/venv/bin/python3
Cron log:         /home/elimaoz99/cron_log.txt

Cron schedule (two runs per day):
  14:00  fetch_raw_messages.py   — fetches Telegram messages
  14:05  fetch_clal_48h.py       — fetches Clal Finance data
  14:10  generate_report.py      — calls Claude, saves stock_recommendations_DD_MM_YYYY_Midday.txt
  17:45  fetch_raw_messages.py
  17:50  fetch_clal_48h.py
  17:55  generate_report.py      — saves stock_recommendations_DD_MM_YYYY_Evening.txt

generate_report.py uses model: claude-sonnet-4-6  (upgraded from Haiku — May 5 2026)

Prompt style rules (added May 5 2026 — do NOT revert):
  - Persona: "כתב TheMarker" (not generic analyst)
  - Forbidden phrases: "מצד אחד... מצד שני", "תופעה המעידה", "יום המאופיין",
    "ביטחון יחסי", "אמירה חיובית", "צמיחה מרשימה", "רקע מורכב"
  - Numbers rule: "+3.2%" not "עלייה חדה"
  - Max 20 words per sentence
  - Section headings locked: "אותות חיוביים" / "אותות שליליים" / "שורה תחתונה"
    (previously "אמירה חיובית חזקה" / "תמצית מנהלים" — caused AI-sounding output)

Same anti-AI rules also applied to local fetch_daily_report.py (May 5 2026).

Output files saved to /home/elimaoz99/stock_predictor/ (same dir as api.py).
/api/recommendations reads all stock_recommendations_*.txt files from that directory.
Sort order: by file mtime descending (newest first) — fixed May 1 2026.

To re-run pipeline manually (on server):
  cd /home/elimaoz99/stock_predictor
  venv/bin/python3 fetch_raw_messages.py
  venv/bin/python3 fetch_clal_48h.py
  venv/bin/python3 generate_report.py

IMPORTANT: fetch_daily_report.py does NOT exist on the server.
It is a LOCAL Mac script at ~/Desktop/Stock-predictor/fetch_daily_report.py
The server uses generate_report.py for production report generation.

Local Mac scripts (~/Desktop/Stock-predictor/):
  fetch_daily_report.py  — standalone, saves to ~/Desktop/daily_reports/, model: claude-sonnet-4-6
                           includes Telegram error alerts + SCP auto-upload to server
                           anti-AI style rules applied May 5 2026 (same as generate_report.py)
  backtest_month.py      — backtests ML model over 3 historical windows

Telegram credentials (.env at ~/Desktop/Stock-predictor/.env and ~/Desktop/daily_reports/.env):
  TG_API_ID, TG_API_HASH, TG_PHONE, TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID
Telegram session file: ~/tg_session/session_v3.session
Telegram sources: capitalflow_co_il, claltelegram, altshuler_shaham, msh_telegram2023 (מגדל — נוסף May 17 2026)
Message truncation (fetch_daily_report.py): 1200 chars per message (raised from 600 — May 17 2026)


SECURITY RULES (enforced — do not revert)
------------------------------------------
- No CORS — frontend is co-hosted, same origin
- Scanner is read-only — no edit inputs, no save endpoint
- Ticker validated by regex ^[A-Z0-9.\-]{1,15}$
- Error messages generic — never expose exception details to client

SECURITY HARDENING (applied May 4 2026 — do NOT revert)
---------------------------------------------------------
Six vulnerabilities were found and fixed. If deploying from an older api.py or models.py,
re-apply all of these.

models.py:
  - market_id whitelist: only "us", "tase", "sp500", "nasdaq100" accepted (field_validator).
    Anything else → HTTP 422 before hitting DB or spawning a thread.
  - top_n clamped: ge=1, le=50. Was unbounded — attacker could request 1,000,000 results.
  - min_confidence clamped: ge=0.0, le=1.0. Was unclamped — negative value bypassed filters.
  - task_id format enforced: alphanumeric/dash/underscore, max 64 chars (_TASK_ID_RE).

api.py:
  - Rate limit now guards ALL scan starts (not just force_refresh=True).
    Old bug: cold-cache + force_refresh=False had zero rate limiting → unlimited thread spawn.
    Fix: rate limit check moved to run before any background scan, regardless of force_refresh.
  - Concurrent scan cap: _SCAN_SEMAPHORE = threading.Semaphore(3).
    Max 3 simultaneous full market scans. 4th gets "Server busy" error, semaphore released in finally.
  - GLOBAL_PROGRESS memory leak fixed: entries now popped from dict when results are consumed
    in get_scan_progress(). Previously grew forever under sustained use.
  - _scan_rate_limit memory leak fixed: entries older than SCAN_COOLDOWN * 2 pruned on each
    scan request. Previously grew with every unique IP that ever hit the endpoint.
  - task_id probe attack fixed: GET /api/scan/progress/{task_id} now validates format with
    TASK_ID_RE before any dict lookup. Returns HTTP 400 on invalid format.
  - Removed duplicate `import threading` from inside scan_market() — already imported at module level.


KEYS (local .env at ~/Desktop/Stock-predictor/.env and ~/Desktop/daily_reports/.env)
--------------------------------------------------------------------------------------
  FRED_API_KEY       FRED macroeconomic data
  GEMINI_KEY         report generation
  ANTHROPIC_API_KEY  Claude API
  TG_API_ID / TG_API_HASH / TG_PHONE   Telegram scraping
  TELEGRAM_BOT_TOKEN / TELEGRAM_CHAT_ID  Telegram bot alerts


ALMOST BUY FEATURE (added May 2 2026)
---------------------------------------
Stocks that ML predicted BUY but options filter penalized below CONFIDENCE_THRESHOLD
appear in a separate amber section in the scanner tab — NOT in the main BUY table.

Fields returned per almost_buy record:
  confidence          post-penalty score (< 0.70)
  original_confidence pre-penalty ML score (what it was before options multiplier)
  almost_buy          True
  options_filtered    True
  options_context     raw options signals (pc_ratio, iv_skew, etc.)

Frontend: amber "⚠️ ALMOST BUY (N)" tab in ScannerView.
  - Shows ML→Conf range (original → penalized), PC Ratio column
  - "סרוק פרטנית →" button: navigates to predict tab, auto-runs individual prediction
  - Individual scan retrains on that stock fresh → often crosses threshold → promoted to BUY

Backend change in _apply_options_filter():
  Old: return None  ← stock disappeared entirely
  New: return {**result, "almost_buy": True, "original_confidence": ..., ...}

api.py filter (both cache path and fresh scan path):
  [r for r in results if r['confidence'] >= req.min_confidence or r.get('almost_buy')]


OPTIONS LIQUIDITY FIX (May 2 2026)
-------------------------------------
fetch_options_features() previously used ATM-only OI (3 strikes) as liquidity check.
Large-cap stocks concentrate put OI far OTM (portfolio hedges) → ATM OI < 100 → null options.

Fix: liquidity check now uses TOTAL put/call OI ≥ 200 per side (not ATM-only).
PC ratio: volume-based (≥50 contracts per strike), falls back to OI if volume too low.
This affects ALL stocks, not just WDC.


COMMON FAILURE MODES & FIXES
------------------------------
Symptom: Endpoint returns 404
Cause:   Frontend updated but backend endpoint missing
Fix:     Add endpoint to api.py, scp, restart

Symptom: API error after restart
Cause:   In-memory cache populated with None (transient parallel fetch failure)
Fix:     Restart service again

Symptom: Changes not live
Cause:   scp ran but service not restarted
Fix:     sudo systemctl restart stock-app.service

Symptom: Key not picked up
Cause:   Key added to api_data.env but service not restarted
Fix:     Restart service

Symptom: Wrong file edited
Cause:   Edited stock_app/ instead of stock_predictor/
Fix:     Always verify path before editing

Symptom: All markets return 0 scan results, every stock fails with shape=(0, 20)
Cause A: FRED_API_KEY not loaded (cron has clean env — pre_scan.py must load api_data.env)
         Verify: check first lines of pre_scan.py load api_data.env
Cause B: FRED API returned HTTP 500 (happens occasionally)
         Fix: core_logic.py fetch_macro_timeseries() has 3-tier fallback:
              1. FRED API direct
              2. DGS10-DGS2 spread (if only T10Y2Y fails)
              3. yfinance ^TNX / ^IRX (if FRED entirely down)
         Verify: run debug_fred.py on server

Symptom: Scanner results unchanged for days
Cause:   pre_scan.py silent failure — check pre_scan.log tail
Fix:     Check last "Pre-scan complete." timestamp; Telegram alert should arrive daily at ~05:05

Symptom: Options data null / pc_ratio missing for a large-cap stock
Cause A: Old bug (pre May 2 2026) — ATM-only OI check too strict; already fixed
Cause B: Stock genuinely has < 200 total OI per side → truly illiquid options
Fix:     Check Yahoo Finance options chain; if liquid, check fetch_options_features() liquidity threshold

Symptom: Scanner returns 0 BUY results (or very few) even though market is up
Cause A: SCAN_CONFIDENCE_THRESHOLD too high relative to light_mode calibration.
         Light mode (100 iter) produces ~0.10 lower confidence than full mode (300 iter).
         If threshold is 0.67, stocks that would score 0.70+ in full mode score ~0.57-0.60 in light.
         Fix: SCAN_CONFIDENCE_THRESHOLD = 0.57 (set May 13 2026)
Cause B: MIN_PRECISION_BUY too high during post-correction regime.
         Test set (last 20% of 5y data) includes correction period → BUY precision scores
         temporarily suppressed (0.30-0.36 instead of 0.48+) because many signals failed in decline.
         Fix: lower MIN_PRECISION_BUY temporarily (0.30 as of May 13 2026); restore ~Nov 2026.
Cause C: CMF filter blocking signals — broad money outflow after correction.
         Fix: check df["cmf"].iloc[-1] for candidate stocks; if mostly negative, regime issue.
Cause D: FRED macro features missing/NaN → model predictions degrade.
         Fix: run debug_fred.py on server to verify macro join is healthy.

Symptom: NASDAQ100 pre_scan shows same signal count as SP500 (e.g. 483 vs 483)
Cause:   load_sp500() and load_nasdaq100() shared same TTLCache object — cache key collision on ()
Fix:     Ensure each function uses its own TTLCache: _sp500_cache, _nasdaq_cache (fixed May 5 2026)
         If this reappears after deploying old core_logic.py, re-apply the separate cache instances.

Symptom: Delisted or merged stock appears in Volume Leaders
Cause:   Yahoo Finance screener returns stale entries for stocks that have been delisted or merged.
         The stock shows historical volume (non-zero) but regularMarketTime is days/weeks old.
         Example: CTRA after Devon Energy merger completed May 7 2026 — still returned by screener
         with 73M volume but last trade was May 6 2026.
Fix:     Filter in get_volume_leaders(): regularMarketVolume > 0 AND regularMarketTime >= now-86400.
         Both checks applied together catch stale entries (fixed May 13 2026).

Symptom: BUY signal dropped from scanner but appears in individual predict
Cause:   Options filter penalized confidence below 0.70 in batch scan.
         Individual predict retrains fresh → slightly different confidence → may cross threshold.
Fix:     Use ALMOST BUY tab in scanner → click "סרוק פרטנית" to confirm via detail scan

Symptom: FRED dashboard shows None for CORE_CPI, FED_FUNDS, CFNAI, M2, or SENTIMENT
Cause:   _fred_obs() was using aggregation_method=eop — FRED returns HTTP 500 for this on monthly series.
         If deploying from an old api.py (pre May 4 2026), this bug will reappear.
Fix:     Ensure _fred_obs() only uses frequency=m&aggregation_method=avg for DGS10/DGS2 (daily=True).
         All other series must NOT pass frequency or aggregation_method. See FRED MACRO DASHBOARD section.

Symptom: Same ticker returns different signal on two consecutive calls (e.g. BUY 79% then HOLD 53%)
Cause:   FRED data availability — t10y2y is a top-5 feature. With FRED available: real values.
         With FRED down (fallback ^TNX-^IRX): different t10y2y → signal can flip entirely.
         Also: core_logic.py caches fetch_stock_data 15min — a fresh Python process gets new data
         while the running API serves the cached result.
Fix:     Run debug_fred.py on server to check FRED status.
         API result is authoritative (uses cached FRED from last successful fetch).

Symptom: Frontend JS/CSS 404 after deploy
Cause:   Only index.html was copied to frontend/dist/ — Vite hashes asset filenames each build.
Fix:     Always cp -r the full dist/ to frontend/dist/ (broke site May 10 2026).


ML MODEL STATE (as of 2026-05-13)
-----------------------------------
File: core_logic.py (local: ~/Desktop/Stock-predictor/, server: /home/elimaoz99/stock_predictor/)
Backup on server: core_logic.py.bak (pre-May-2 options fix)

LONG-ONLY EXPERT (applied May 12 2026):
  - CONFIDENCE_THRESHOLD raised 0.65 → 0.70 (OOS backtest: 49.4% precision / +2.96% avg ret at 0.70)
  - SELL signal suppressed: get_prediction() and run_market_scan() convert SELL→HOLD before return
  - Model still trains on 3 classes (BUY/SELL/HOLD) — suppression is output-only, not label change
  - Binary BUY/NOTBUY was tested and REJECTED (precision dropped to 40.8% — 3-class is better calibrated)
  - Do NOT re-enable SELL output. Do NOT revert threshold to 0.65. Do NOT change to binary labels.
  - _compute_verdict() in api.py also updated: thresholds 0.65/0.70 (high-vol/normal), SELL→HOLD (now compute_verdict in scanners.py — Jun 7 2026 refactor)

SCANNER THRESHOLD FIX (applied May 13 2026):
  - SCAN_CONFIDENCE_THRESHOLD lowered 0.67 → 0.57
    Reason: light mode (100 iter) produces ~0.10 less confidence than full mode (300 iter).
    The original 0.03 offset was insufficient. Confirmed empirically: NVDA light=0.58, full=0.69.
    The 0.57 scanner threshold is equivalent to ~0.67 in full mode — the correct comparison level.
    Do NOT raise back to 0.67 — it will produce 0 BUY results in normal market conditions.
  - MIN_PRECISION_BUY lowered 0.48 → 0.30 (TEMPORARY — restore to 0.48 ~Nov 2026)
    Reason: test set (last 20% of 5y = ~1 year) includes the April 2026 tariff-war correction.
    The correction artificially suppresses BUY precision in the evaluation window
    (many BUY signals that fired during the decline registered as failures).
    When ~6 months of post-correction data accumulates, restore to 0.48.

Config:
  PERIOD                    = "5y"
  FORWARD_DAYS              = 10
  THRESHOLD                 = 0.03    (3% move = BUY label)
  CONFIDENCE_THRESHOLD      = 0.70    (full predict, 300 iter)
  SCAN_CONFIDENCE_THRESHOLD = 0.57    (scanner light_mode, 100 iter; ~0.10 below full mode)
  MIN_PRECISION_BUY         = 0.30    (TEMP — restore to 0.48 ~Nov 2026 when correction exits test window)
  MIN_PRECISION_SELL        = 0.52
  MIN_PRECISION             = 0.52    (alias used by backtest scripts)

FEATURES (20 total — technical + macro + relative strength; order matters):
  0  ema9              short-term EMA
  1  ema21             crossover signal
  2  ema50             institutional trend
  3  ema_cross         ema9 > ema21 flag (0/1)
  4  rsi               RSI(14)
  5  macd_gap          MACD histogram
  6  bb_pos            Bollinger Band position (0=lower, 1=upper)
  7  vol_ratio         today volume / 20d avg
  8  ret_3d_atr        3d return / ATR(14) — cross-stock normalized
  9  ret_5d_atr        5d return / ATR
  10 ret_10d_atr       10d return / ATR
  11 sma200_dist       (Close - SMA200) / SMA200
  12 low52w_dist       (Close - 52w_low) / 52w_low  ← bounce detector (added Apr 27)
  13 atr_pct           ATR(14) / Close — volatility regime
  14 vix               CBOE VIX (from yfinance ^VIX)
  15 dgs10             10Y Treasury yield (FRED DGS10, fallback: yfinance ^TNX)
  16 t10y2y            Yield curve 10Y-2Y (FRED T10Y2Y, fallback: DGS10-DGS2 or ^TNX-^IRX)
  17 rel_strength_spy  5d return minus SPY 5d return
  18 cmf               Chaikin Money Flow (20-period) — money flow direction
  19 rel_strength_sector  5d return minus own sector ETF 5d return

NOTE: Options features (pc_ratio, iv_skew, volume_shock) are used as a POST-PREDICTION
filter (in _apply_options_filter / get_prediction), NOT as model input features.
OPTION_FEATURES = {"pc_ratio", "iv_skew", "volume_shock"} is a separate set in core_logic.py.

INTERACTION_GROUPS (7 groups — passed as interaction_cst to HistGBT):
  [0,1,2,3,11,12,17,19]        trend + distance + relative strengths (SPY + sector)
  [4,5,6,18]                   momentum + CMF
  [7,8,9,10,13,18]             returns + volume + ATR + CMF
  [4,5,6,14,15,16]             momentum × macro
  [8,9,10,13,14,15,16,17,19]   returns_atr × macro + relative strengths
  [4,6,12]                     RSI + bb_pos + low52w_dist  ← oversold bounce detector (added Apr 27)
  [4,17,19]                    momentum + sector vs market divergence

Classifier: HistGradientBoostingClassifier
  max_iter=300 (full), 100 (scanner light_mode)
  max_leaf_nodes=31, min_samples_leaf=50
  l2_regularization=0.1, learning_rate=0.01
  early_stopping=True, n_iter_no_change=20

Precision reporting:
  - Uses signal-specific precision: SELL signal → SELL precision, BUY signal → BUY precision
  - MIN_SUPPORT = 5: if test set has fewer than 5 predictions for that class → precision = 0
  - Precision capped at 90% to prevent 1-sample 100% display artifact


CHAOS FILTER (meme/high-vol stock exclusion)
---------------------------------------------
Applied in both _train_single() (scanner) and get_prediction() (single lookup).
Rule: avg ATR over last 30 trading days > chaos_threshold → skip
  chaos_threshold is VIX-based (dynamic):
    VIX < 15  → 5%
    VIX < 25  → 7%
    VIX >= 25 → 10%   (high-vol regime: legitimate stocks spike ATR; looser threshold needed)
  - Scanner: silently returns None
  - Single lookup: returns signal="EXCLUDED" with excluded_reason field
Rationale: 10-day swing model is structurally wrong fit for meme stocks (HOOD, GME, etc.)
The dynamic threshold was added because during corrections (VIX>25), many legitimate stocks
temporarily exceed 7% ATR and would be excluded incorrectly without regime-aware thresholding.


MACRO PREDICTED PAGE (5th tab)
--------------------------------
Endpoint: GET /api/macro-score  (cache: 2h)
Returns a bull_score 0–100 derived from 14 macro indicators:
  VIX, YIELD_CURVE, CPI, CORE_CPI, PCE, UNRATE, PAYROLLS,
  FED_FUNDS, M2, CFNAI, SENTIMENT, BTC, OIL, GOLD

Scoring: each indicator scored -100 to +100 via tier lookup (_bull_score_item).
Weights sum to 100. Final: (weighted_avg + 100) / 2 → [0, 100].
Regime labels: ≥75 Strong Bull, ≥60 Moderate Bull, ≥45 Neutral,
               ≥30 Cautious Bear, else Strong Bear.

Known bug: BZ=F (Brent crude) sometimes returns stale/wrong price via yfinance.
           Not yet fixed.

Frontend: MacroPredictedView + MacroScoreCard components at end of App.jsx.
Gauge: gradient bar red→green with needle at score position.


DEPLOY SHORTCUT (server has no git — use curl)
------------------------------------------------
The server directory is NOT a git repo. Deploy via curl, not git pull:

  curl -o /home/elimaoz99/stock_predictor/core_logic.py \
    "https://raw.githubusercontent.com/doctor-magic/Stock-predictor/main/core_logic.py"

  curl -o /home/elimaoz99/stock_predictor/api.py \
    "https://raw.githubusercontent.com/doctor-magic/Stock-predictor/main/api.py"

  sudo systemctl restart stock-app.service

SSH works fine with direct IP (35.239.74.178) using the gcp_stock_rsa key.
Claude can initiate SSH connections in non-interactive mode (confirmed May 2 2026).


LEUMI OPTIONS TAB (added May 3 2026)
--------------------------------------
Tab 6: "אופציות לאומי" — Bank Leumi employee stock options simulator (IBI Capital plan).

Plan structure:
  Annual bonus → 80% salary raise + 20% employee buys Series 1 options
  Bank matches 20% employer contribution → Series 2a + Series 2b (split evenly)
  Series 1:  employee-funded, immediately vested
  Series 2a: employer-funded, 1yr vest
  Series 2b: employer-funded, 2yr vest
  Exercise: cashless (מימוש נטו) — shares = options × (price − strike) / price

UI inputs:
  מענק שנתי (₪)          — annual bonus amount
  מחיר מימוש (₪)         — strike price (default: 75 ₪ ≈ LUMI.TA current price)
  שווי אופציה — סדרה 1   — Black-Scholes value per option (default: 18 ₪)
  שווי אופציה — סדרה 2   — Black-Scholes value per option (default: 20 ₪)
  מחיר מניה נוכחי (₪)    — current/future stock price
  משכורת שנתית (₪)       — annual salary for marginal tax bracket calculation (default: 240,000)

Tax calculation:
  Uses Israeli income tax brackets (IL_BRACKETS in App.jsx) — NOT a flat rate.
  Brackets: 10% / 14% / 20% / 31% / 35% / 47% (2025 rates).
  Tax on options profit = calcIncomeTax(salary + profit) − calcIncomeTax(salary)
  This gives the MARGINAL tax on the option income only.
  Tax type: הכנסת עבודה (employment income), NOT capital gains (25%).
  The IBI plan is income-track — marginal rates apply at exercise.

Black-Scholes reference values (LUMI.TA, May 2026):
  Stock price: ~75 ₪ (LUMI.TA on TASE, quoted in agorot ÷ 100)
  σ ≈ 38%, r = 4.5%, q = 3.5% (dividend yield)
  Series 1 (T=3yr): ~18 ₪  ← default in simulator
  Series 2a (T=4yr): ~20 ₪ ← default in simulator
  Series 2b (T=5yr): ~22 ₪ (note: simulator uses 20 ₪ for both series 2 tranches)

Display: 3 stat cards (ברוטו / מס שולי / אחרי מס) + sensitivity table by price scenario.
Components: LeumiOptionsView, OptionsInput, OptionsStat + calcIncomeTax() + IL_BRACKETS[]
  all defined at end of App.jsx.


BACKTEST SCRIPT
---------------
File: ~/Desktop/Stock-predictor/backtest_month.py
Run:  python3 backtest_month.py  (from ~/Desktop/Stock-predictor/)

3 test periods:
  May 2025 (calm bull):   train up to 2025-05-30, check 2025-06-16
  Jan 2026 (normal):      train up to 2026-01-31, check 2026-02-14
  Mar 2026 (Black Swan):  train up to 2026-03-26, check 2026-04-09

Last backtest results (2026-04-27, with low52w_dist feature):
  Combined: 6 signals, 3/6=50% hit rate, avg precision 79.6%
  Jan 2026:  5 signals, 3/5=60% — best window
  Black Swan: 0 signals — all filtered by MIN_PRECISION
  AAPL/NVDA: no signals in any period (below precision threshold)
  SELL dominates — no BUY signals appeared in any window

Script mirrors core_logic.py exactly (same FEATURES, INTERACTION_GROUPS,
MIN_PRECISION, chaos filter). Keep them in sync when updating the model.


WALK-FORWARD OOS BACKTEST (May 12 2026)
-----------------------------------------
File: ~/Desktop/Stock-predictor/swing_backtest.py
Run:  python3 swing_backtest.py  (from ~/Desktop/Stock-predictor/)

Method: expanding window, 25 stocks, 4 OOS quarters (TRAIN_MIN_DAYS=252, STEP_DAYS=63).
Imports core_logic directly — always reflects production model state.

Key findings (May 12 2026):
  3-class BUY >= 0.70: 344 signals, 49.4% precision, +2.96% avg fwd ret  ← current production
  3-class BUY >= 0.65: 501 signals, 47.3% precision, +2.75% avg fwd ret  ← old production
  Binary BUY >= 0.65:  306 signals, 40.8% precision, +2.35% avg fwd ret  ← REJECTED (tested, inferior)
  SELL signal OOS:     +1.30% avg fwd ret (positive on "down" predictions) ← broken, suppressed

Per-symbol edge (BUY signals, 3-class model):
  Strong edge: CAT (59.6%), AMD (59.6%), BAC (53.8%), GS (52.4%), XOM (52%)
  No edge:     CRM (22.5%), COST (25%), JPM (37.3%) — model fires too often with poor precision


LIVE TRACKER (added May 12 2026)
----------------------------------
File: ~/Desktop/Stock-predictor/live_tracker.py
DB:   ~/Desktop/Stock-predictor/tracker.db  (local SQLite — do NOT commit to git)
      ⚠️ SUPERSEDED Jun 14 2026: live_tracker.py now runs as a SERVER cron (20:05 UTC Mon–Fri).
      The live DB is /home/elimaoz99/stock_predictor/tracker.db. The Mac copy is legacy — do NOT
      scp it over the server (overwrote 15 rows on Jun 14). The local --log/--report commands below
      are historical; run them on the server now. See 'JUN 14 — LIVE TRACKER HARDENING' at end.

Purpose: Validate Long-only Expert (>=0.70) live OOS performance.
Pulls daily BUY signals from GCP scan API, resolves outcomes after 10 trading days via yfinance,
sends Telegram digest with running precision and avg return.

Usage:
  python3 live_tracker.py --log              # after market close (16:30 ET+) — logs + resolves + Telegram
  python3 live_tracker.py --report           # print current stats, no API call
  python3 live_tracker.py --log --no-telegram

Data flow:
  1. POST /api/scan (market_id=sp500 + nasdaq100, min_confidence=0.70, force_refresh=False)
  2. Filter signal == "BUY" (server already applies confidence filter)
  3. Deduplicate by sym — same stock in both markets counted once
  4. entry_price = last_price from scan result
  5. exit_price  = yfinance close at np.busday_offset(date_logged, +10) days
  6. hit = 1 if (exit_price / entry_price - 1) >= 0.03

Telegram digest format:
  New BUY signals (N): sym, conf%, entry price
  Resolved today (N):  [+] sym +X.XX% / [-] sym -X.XX%
  Validation (N signals resolved): Precision X.X%, Avg ret +X.XX%, Pending N

Note: pre-scan cron (05:00) must have run before --log for cache to be fresh.
First clean day with CONFIDENCE_THRESHOLD=0.70 in cache: 2026-05-13.


VOLUME LEADERS TAB (added May 10 2026)
----------------------------------------
Tab 7: "Volume Leaders" — most active US stocks by volume, filtered by market cap,
analyzed with ML signal + momentum indicators.

Purpose:
  High-volume stocks often signal momentum opportunities. This tab finds them daily,
  filters noise (mkt cap < $200M), and layers the existing ML model on top.

Backend endpoint: GET /api/volume-leaders?min_market_cap=200000000&force=false
  File: api.py — function get_volume_leaders()
  Cache: 30 min (_volume_leaders_cache). Pass ?force=true to bypass.

Data pipeline:
  1. Yahoo Finance unofficial screener API (query1.finance.yahoo.com) — top 50 most active
  2. Filter: marketCap >= $200M + regularMarketVolume > 0 + regularMarketTime within last 24h
     → keeps up to 20 stocks
     NOTE: Yahoo Finance sometimes returns stale entries for delisted/merged stocks (e.g. CTRA
     after Devon Energy merger May 7 2026) with non-zero historical volume but last trade days
     or weeks old. The regularMarketTime >= now-86400 filter catches these.
  3. ML signal: query scanner_cache.db for today's sp500 + nasdaq100 pre-scan results
     (2 queries total, dict lookup — no N+1 loop)
  4. yfinance batch download (6mo daily — was 1mo; raised May 18 2026 for SMA50 + 100-bar ATR/regime window) → RSI(14) + 5-day return per ticker
  5. Volume ratio: regularMarketVolume / averageDailyVolume3Month (from screener data)

Verdict logic (compute_verdict in scanners.py — moved + renamed from api.py's _compute_verdict in the Jun 7 2026 refactor):
  OVEREXTENDED — price up ≥3% from open (anti-chasing guard)
  VOL BREAKOUT — vol_ratio ≥ 3x + RSI < 65 (independent of ML)
  BUY          — ML=BUY + conf ≥ 0.65 (if vol_ratio ≥ 2x) or ≥ 0.70 (normal)
  HOLD         — everything else incl. ML=SELL (SELL suppressed — Long-only Expert)
  WATCH        — RSI < 35 + vol_ratio ≥ 2x (momentum only, no ML)

  NOTE: ML=SELL → HOLD (SELL suppressed since May 12 2026 — Long-only Expert)

Response format (wrapped object — NOT a plain array):
  { "results": [...], "fetched_at": "2026-05-10T18:32:00Z" }
  Frontend uses json.results and json.fetched_at separately.

Frontend component: VolumeLeadersView() at end of App.jsx
  Table columns: Ticker | Name | Price | Mkt Cap | Volume | Vol/Avg | 5d Ret | RSI | ML Signal | Verdict
  Vol/Avg highlight: ≥ 3x → orange bold + 🔥, ≥ 2x → yellow, < 2x → gray
  Timestamp: shows server-side fetched_at (not client time) — yellow ⚠ warning if data is from previous day
  Refresh button: calls ?force=true to bypass 30-min cache

Known limitations:
  - Yahoo Finance screener is an unofficial API — can break without notice
  - ML signal is N/A for stocks outside S&P 500 / NASDAQ 100 (e.g. NOK, IREN, RKLB)
  - Verdict for non-indexed stocks relies only on RSI + volume ratio
  - Service MUST be restarted after deploy for endpoint to be available
    (sudo systemctl restart stock-app.service)

Deploy note (critical):
  After every frontend build, copy FULL dist folder — not just index.html:
    cp -r /home/elimaoz99/stock_predictor/dist/. /home/elimaoz99/stock_predictor/frontend/dist/
  Vite generates new hashed filenames on each build. Copying only index.html
  causes 404 on JS/CSS assets (broke the site on May 10 2026).


VOLUME LEADERS — MOMENTUM GATES (added May 22 2026)
-----------------------------------------------------
Two filters that suppress BUY setup signals when intraday momentum is exhausted.
Model philosophy: Volume Leaders is a MOMENTUM CHASER. Gates answer "is the fuel still alive?"
Do NOT add support/resistance level detection — that changes the model's identity.

Gate 1 — HOD Gap / ATR-14:
  Active window: is_live=True AND 10:00 <= ET_hour < 16:00 only.
    Before 10:00: ORB forming, HOD is ephemeral → gate disabled.
    After 16:00:  post-market thin volume inflates HOD ratio → gate disabled.
  hod = quote.get("regularMarketDayHigh") from Yahoo screener (same call, no extra fetch).
  atr14_val from _atr_daily_cache[sym] — populated by pandas ewm(alpha=1/14) on 6mo daily hist.
    Cache key: (sym, trading_date_ET). On cache miss: compute and store. On hit: O(1) lookup.
    Do NOT compute ATR inline per-request — the 6mo download is the expensive part, ATR piggybacks.
  hod_gap_ratio = (hod - current_price) / atr14_val
  Gate fires when hod_gap_ratio > 0.35 → setup = None, setup_blocked_by = "HOD"
  Threshold 0.35 is a starting point. Calibrate after ~50 resolved signals:
    SELECT hod_gap_atr_bucket, AVG(fwd_ret_10d) FROM signals GROUP BY 1

Gate 2 — RVOL Slope:
  Global state: _rvol_history: dict[str, deque(maxlen=3)] — newest entry at index 0.
  SLOT GUARD (critical): before appendleft, check (now - deque[0].ts) >= 270 seconds.
    If same 5-min slot: update deque[0] in-place — do NOT appendleft.
    Without this guard: 3x F5 refresh fills deque with identical readings → flat slope → wrong signal.
  Requires 3 readings (~15 min post-10:00) before slope is valid.
  Fires when: RVOL_now < mean(T-1, T-2) * 0.95 → rvol_trend = "down" → setup suppressed.
  setup_blocked_by = "RVOL"

API additions: setup_blocked_by ("HOD"/"RVOL"/None), hod_gap_ratio (float), rvol_trend ("up"/"flat"/"down"/None)

Frontend (App.jsx):
  RVOL cell: inline ▲/▼/→ arrow (green/red/gray) appended after the "x" value. Zero new columns.
  SETUP cell: "— ⊘" with native HTML title tooltip in Hebrew when blocked. Plain "—" otherwise.
    title HOD:  "חסום: HOD gap {ratio}× ATR — מומנטום מותש"
    title RVOL: "חסום: RVOL דועך — דלק אוזל"

Calibration case study (IONQ May 21 2026):
  Entry 1 (10:43 ET, $58.04, HOD $61, ATR ~$4): ratio 0.74 → BLOCKED (missed +$30 winner)
  Entry 2 (13:25 ET, $59.18, HOD $61): ratio 0.455 + rvol_trend=down → BLOCKED (avoided -$52 stop-out)
  Net: $0 with gates vs -$22 without gates.
  Lesson: 0.74 blocked a winner — correct statistically, wrong once. 0.35 threshold needs calibration.


VOLUME LEADERS — ON-DEMAND ML (added May 10 2026)
---------------------------------------------------
Enhancement to the Volume Leaders tab: stocks outside S&P 500 / NASDAQ 100
(e.g. IREN, NOK, RKLB) now receive a live ML prediction instead of showing N/A.

How it works:
  After loading sp500_map + nasdaq_map from scanner_cache.db, collect uncached tickers.
  Run core_logic.get_prediction(sym, light_mode=True) on all of them in parallel via
  ThreadPoolExecutor(max_workers=5). One failure per stock is isolated — won't crash
  the endpoint. Results merge into live_predictions dict.

  In the per-stock loop:
    ml = sp500_map.get(sym) or nasdaq_map.get(sym)
    ml_live = False
    if not ml and sym in live_predictions:
        ml = live_predictions[sym]
        ml_live = True

  Result dict includes "ml_live": True/False.
  "EXCLUDED" signal (chaos filter: ATR too high) is treated as N/A.

  light_mode=True: matches the scanner (max_iter=100, faster, same accuracy).
  ThreadPoolExecutor is already imported at top of api.py.

Frontend:
  ML Signal cell: shows ⚡ next to signal when ml_live=True.
  ⚡ has title tooltip: "Signal computed live — stock is outside the regular
  S&P 500 / NASDAQ 100 watchlist"
  Bottom legend: "⚡ = signal computed live on-demand — stock is outside
  the regular S&P 500 / NASDAQ 100 watchlist."

Performance:
  ~2-5 sec added to first load if uncached stocks exist.
  30-min endpoint cache means this runs once per half-hour, not per user.


EARNINGS CALENDAR (added May 18 2026)
--------------------------------------
Endpoint: POST /api/earnings-calendar
Body:     {"symbols": ["AAPL", "NVDA", ...]}  (max 300 symbols, validated by TICKER_RE)
Returns:  {symbol: {"date": "YYYY-MM-DD", "days_until": int}} — only symbols with upcoming earnings

Implementation (api.py):
  _get_next_earnings(symbol) → fetches yfinance Ticker.info["earningsTimestamp"]
    Converts Unix timestamp to UTC date. Returns None if past or not found.
  _earnings_cache: dict[str, dict] — per-symbol cache {"result": ..., "ts": float}
  _EARNINGS_CACHE_TTL = 6 * 3600 (6 hours)
  ThreadPoolExecutor(max_workers=10) for parallel fetching
  EarningsRequest Pydantic model (symbols: List[str])

Frontend (ScannerView in App.jsx):
  fetchEarnings(scanResults) — called after every scan result:
    cache hit (data.status === 'done')
    poll done (pData.done && pData.results)
    fallback (Array.isArray(data))
  Badges shown in Symbol cell of both main table and ALMOST BUY table:
    ⚠️ Xd (red bg-red-500/20)    — days_until <= 7  (danger: report imminent)
    📅 Xd (yellow bg-yellow-500/20) — days_until <= 14 (heads up)
  title attribute shows full date on hover ("Earnings: 2026-05-20")

IMPORTANT: Model NOT changed — this is UI-only. ML confidence is unaffected by proximity to earnings.
The badge is a manual warning for the user to decide. A future enhancement could auto-reject
BUY signals with earnings within N days (similar to the options filter in core_logic.py).

Example response: {"NVDA": {"date": "2026-05-20", "days_until": 3}}
Test: curl -s -X POST https://stock-predictor.online/api/earnings-calendar \
        -H "Content-Type: application/json" -d '{"symbols": ["NVDA", "AAPL"]}' | python3 -m json.tool


WEDGE SCAN TAB (added May 14 2026, recovered May 18 2026)
-----------------------------------------------------------
Tab 8: "Wedge Scan" — falling wedge pattern results from the overnight pre_scan.py run.
Frontend component: WedgeScanView() in App.jsx.
Endpoints used:
  GET /api/wedge-scan  — reads wedge_cache.json (written by pre_scan.py at 05:00 daily)
  GET /api/wedge-live  — returns today's intraday % change for each wedge symbol

IMPORTANT — git state note (May 18 2026):
  The Wedge Scan tab was accidentally lost when a deploy used an older App.jsx (pre-stash).
  It was recovered by restoring from git stash ref fa7bc56:frontend/src/App.jsx.
  The current App.jsx is NOT committed to git — it contains stash content + earnings calendar changes.
  Before the next major deploy, commit the reconciled App.jsx to GitHub to avoid losing it again.


MAC SETUP (completed May 11 2026)
-----------------------------------
Migrated from Windows to MacBook. All paths below are Mac equivalents.

  SSH key:          ~/.ssh/gcp_stock_rsa  (chmod 600)
  Git repo:         ~/Desktop/Stock-predictor/  (branch: main)
  .env (API keys):  ~/Desktop/Stock-predictor/.env
                    ~/Desktop/daily_reports/.env  (used by fetch_daily_report.py)
  Telegram session: ~/tg_session/session_v3.session
  Daily reports:    ~/Desktop/daily_reports/stock_recommendations_*.txt

fetch_daily_report.py was updated: all hardcoded Windows paths replaced with Path.home()-based paths.


VOLUME LEADERS — BETA GATE (added May 23 2026)
------------------------------------------------
Suppresses ML BUY on high-beta stocks. The ML model is Mean-Reversion on institutional stocks;
it consistently fails on momentum-driven names.

OOS validation (backtest_month.py, May 23 2026):
  Large-Cap  (beta ~0.7-1.2): 3/4 BUY signals correct, avg return +11.7%
  High-Beta  (beta  > 1.5):   0/5 BUY signals correct, avg return -10.8%

Implementation (api.py get_volume_leaders()):
  _BETA_HIGH_THRESHOLD = 1.5  (constant, near TTL constants — do NOT remove)
  SPY downloaded once per call: yf.download("SPY", period="6mo") → spy_returns = pct_change()
  Per-symbol: beta = Cov(stock_returns, spy_returns) / Var(spy_returns) over 6mo aligned window.
    Requires ≥60 bars after inner-join alignment. Wrapped in try/except: beta = None on failure.
  SPY download also in try/except: spy_returns = None → gate disabled gracefully.
  If beta > 1.5 AND verdict == "BUY" → verdict = "HIGH-BETA", beta_blocked = True
  API response adds: beta (float|null), beta_blocked (bool)

Frontend (App.jsx VolumeLeadersView):
  SETUP cell shows "β ⊘" in purple (text-purple-400) with Hebrew tooltip when row.beta_blocked.
  Pattern consistent with HOD/RVOL gates.

Calibrate threshold after ~50 resolved trades:
  SELECT CASE WHEN beta > 1.5 THEN 'high' ELSE 'normal' END, AVG(hit), COUNT(*)
  FROM signals WHERE beta IS NOT NULL GROUP BY 1


VOLUME LEADERS — POWER HOUR WHALE ALERT (added May 27 2026)
-------------------------------------------------------------
Concept: institutional players accumulate beaten-down stocks at daily lows during 15:00-16:00 ET.
Validated: QBTS (May 26), WOLF/PDD/NVTS/QCOM all surged in final 15-20 min on May 27.

Implementation (api.py get_volume_leaders()):
  pct_from_low = (price - regularMarketDayLow) / price * 100  (no extra API calls)
  reversion_alert = is_live AND ET_hour == 15 AND pct_from_low < 2.0 AND rvol_trend != "down"
    Time gate strictly ET_hour == 15 — do NOT extend to 14:xx.
    Alert fires on rvol_trend=None (warming up) — only suppressed on "down".
    pct_from_low < 2.0 threshold — do not loosen above 3% without evidence.
  API response adds: pct_from_low (float|null), reversion_alert (bool)

Frontend (App.jsx VolumeLeadersView):
  Red animate-ping dot in Symbol cell when row.reversion_alert === true.
  Same CSS pattern as Reversion Hunter RVOL alert.
  Tooltip: "🚨 Power Hour — X.X% מהתחתית, נפח עולה"

Slot guard (critical): _rvol_history deque slot guard = 270s — do NOT remove.
  Prevents F5 spam corrupting RVOL slope (which gate depends on).


REVERSION HUNTER TAB (added May 26 2026)
------------------------------------------
Tab 9: "Reversion Hunter" — Yahoo Finance day_losers, stocks down ≥5% with mean-reversion analysis.
Frontend component: ReversionView() at end of App.jsx.
Endpoint: GET /api/reversion-leaders?min_market_cap=500000000&force=false

Data pipeline:
  Yahoo Finance day_losers screener (scrIds=day_losers — same API pattern as most_actives).
  Filter: marketCap ≥ $500M, volume ≥ 2M shares, last trade < 24h, change_pct ≤ -5%.
  ML: query scanner_cache.db first; on-demand get_prediction(sym, light_mode=True) for uncached.
  ThreadPoolExecutor(max_workers=5) for on-demand ML. 30-second timeout.
  yfinance batch download (period="6mo") → RSI(14), regime classification.
  Intraday signals: _get_intraday_signals() → VWAP, RVOL, base_forming.
  Cache: _reversion_cache dict, _REVERSION_TTL = 900 (15 min). ?force=true bypasses.

Verdict logic:
  DEEP BUY:        ml_signal=="BUY" AND RSI < 35 AND vwap_gap_pct < -2%
  POTENTIAL BOUNCE: ml_signal=="BUY" AND (RSI<35 OR vwap_gap_pct<-2%)
  OVERSOLD:        RSI < 35 AND vwap_gap_pct < -2% (no ML BUY needed)
  WATCH:           default
  FALLING KNIFE:   any of above downgraded if base_forming is explicitly False (not None)

CRITICAL — field names differ from Volume Leaders (do not confuse):
  ml_signal (NOT ml_verdict)
  reversion_verdict (NOT verdict)
  ml_confidence: already 0-100 scale (backend does round(conf * 100, 1)).
    Do NOT multiply by 100 in frontend — produces "4520%" display bug.

API response fields (full list):
  symbol, name, price, change_pct, volume, market_cap, rsi, ml_signal, ml_confidence,
  ml_live, vwap, vwap_gap_pct, above_vwap, is_live, rvol, rvol_quality,
  rvol_alert, reversion_verdict, regime, adx, base_forming

rvol_alert (added Jun 2 2026):
  bool = (rvol or 0) > 5.0
  Frontend: red animate-ping dot in Symbol cell, identical CSS to Power Hour alert.
  Tooltip: "🚨 RVOL חריג — Nx נפח גבוה במיוחד"
  Triggered by SOC 9.3x RVOL incident (Jun 1 2026) — wasn't flagged by algorithm.

Entry rule (critical): VWAP bounce confirmation required — do NOT enter on signal alone.
  VWAP is the TARGET, not the entry trigger.
  On headwind days (SPY+QQQ both below VWAP): require confirmed green 5m candle closing above VWAP.
  "+0.2% above VWAP" at entry is NOT confirmation (PDD loss of -$20.44 confirmed this, May 27).

Frontend tables (all 5 symbol tables have Yahoo Finance + TradingView links):
  Yahoo Finance: https://finance.yahoo.com/quote/${symbol}
  TradingView:   https://www.tradingview.com/chart/?symbol=${symbol}  (no exchange prefix needed)
  Symbol cell wrapper: flex items-center gap-1.5 whitespace-nowrap
    NOT inline-flex — inline-flex inside td renders as block, stacks children vertically.

Helper: _clean_rev(obj) replaces nan/inf → None recursively before JSON return.


WEDGE SCAN — TOUCHES COLUMN (added Jun 1-2 2026)
--------------------------------------------------
Shows how many times price touched each trendline — critical context for compression reliability.

Problem that motivated this: GIS showed 99% compression (lines nearly converging) with only
3 touch points (minimum). With few touches, linear regression can produce extreme convergence
that doesn't reflect visual reality.

Backend (pre_scan.py _detect_falling_wedge()):
  upper_touches (ut) and lower_touches (lt) were already computed — not returned.
  Added to return dict: "upper_touches": ut, "lower_touches": lt (after "lookback_used")
  Added to results.append() in _scan_wedge_patterns(): "upper_touches": w.get("upper_touches", 0)

Frontend (App.jsx WedgeScanView):
  New "Touches" column (hidden sm:table-cell) between "Comp." and "Vol".
  Format: "▲N ▼N" (upper / lower touch count).
  Color: green ≥5 touches, yellow =4, gray =3 (minimum — low reliability).
  Data shows "—" until next pre_scan runs at 5:00 AM (zero overhead — computed already).


FALLING KNIFE RESEARCH (started Jun 2 2026)
--------------------------------------------
Testing hypothesis: stocks classified FALLING KNIFE by Reversion Hunter bounce in Power Hour
(15:00→close) due to short covering.

Backtest script (local Mac):
  ~/Desktop/Stock-predictor/falling_knife_backtest.py
  Method: yfinance 5-min bars, 60 days, 54 tickers.
  FALLING KNIFE proxy: intraday drop ≥ 8% by 14:30 ET.
  Measures: price at 15:00 ET → close price = Power Hour return.
  Results (Jun 2 2026, N=11 events):
    Mean PH return: +1.02% (vs control +0.11%)
    Median:         +1.47%
    Win rate:       54.5%
    FK vs Control:  t=3.13, p=0.0018 ✅ statistically significant
    vs zero:        p=0.067 (marginal — N too small)
  Full results CSV: ~/Desktop/Stock-predictor/falling_knife_results.csv

Automated logging (api.py):
  falling_knife_log.db — SQLite, server-side (/home/elimaoz99/stock_predictor/)
  Tables:
    fk_events: id, symbol, date, classify_ts, price, change_pct, rsi, rvol,
               vwap_gap_pct, price_ph, price_close, ph_return, resolved
    fk_milestones: key, ts (used for one-time alert sentinel)
  _fk_log_event(): called when FALLING KNIFE classified during 13:00–15:00 ET
  _fk_resolve_yesterday(): fetches close prices via yfinance for unresolved past events
  Endpoint: GET /api/falling-knife-stats → resolves + returns events + stats
  Milestone alert: Telegram sent ONCE when resolved events first cross 30.
    Recorded in fk_milestones table as key='n30_alerted' — never fires again.
    Message includes mean return, win rate, reminder to run RSI analysis.

ANTI-HARKING RULE (do not add RSI filter prematurely):
  TTD (RSI 71.8 at drop time) was the worst FK event (-1.53% PH return, Jun 2 2026).
  Tempting to add RSI < 40 filter — but that's HARKing (fitting rule to N=1 outlier).
  Log ALL events regardless of RSI. After 30+ resolved events, run:
    SELECT CASE WHEN rsi < 40 THEN 'low_rsi' ELSE 'high_rsi' END,
           AVG(ph_return), COUNT(*)
    FROM fk_events WHERE resolved=1 GROUP BY 1
  Only add filter if the split is statistically significant.

Power Hour retrospective (Jun 2 2026, 15:31 ET → close):
  CBOE: $273.10 → $275.59 (+$2.49) ✅ whale accumulation confirmed, LOD $269.96
  SHAK: $56.56  → $57.01  (+$0.45) ✅
  MIAX: $42.30  → $42.51  (+$0.21) ✅ (RVOL 7x alert)
  MSTR: $134.44 → $136.08 (+$1.64) ✅ (despite FALLING KNIFE)
  CELC: $89.33  → $91.42  (+$2.09) ✅ (despite FALLING KNIFE)
  INTU: $324.00 → $322.14 (-$1.86) ✗ — already above VWAP at 15:31, faded into close
  Lesson: "already above VWAP" ≠ bullish in Power Hour. Look for stocks near VWAP building base.


JUN 7 2026 — ARCHITECTURE REFACTOR (commit 9fe3557)
=====================================================
This is the most structurally significant change since the project started.
Read this section before touching any backend file.

FILE STRUCTURE AFTER REFACTOR:
  api.py       1387 lines (was 2187). Thin routing + macro/VIX logic only.
               Imports: `import scanners` and `import db as _db`
  scanners.py   520 lines (NEW FILE). All scanner computation helpers.
  db.py         316 lines (was 74). All SQLite logic for all 3 databases.

DEPLOYMENT RULE (hard — do not break):
  Always deploy api.py + scanners.py + db.py together in one scp command.
  api.py imports from both at module load. Partial deploy = ImportError = site down.

WHERE TO ADD NEW CODE:
  New scanner gates, verdict logic, intraday analysis → scanners.py
  New SQLite tables, logging functions, persistence   → db.py
  New API endpoints, macro/VIX logic, routing         → api.py
  New cron scripts or local tools                     → standalone files
  NEVER move scanner logic back into api.py — circular import risk.

SETUP OUTCOME LOGGER (added Jun 7 2026):
  setup_log.db — tracks all scanner signals for outcome measurement.
  Source field: "volume_leaders" / "reversion_hunter" / "gainers"
  Volume Leaders: logs all verdicts EXCEPT HOLD and N/A
  Reversion Hunter: logs DEEP BUY and POTENTIAL BOUNCE
  Gainers: logs all verdicts EXCEPT WATCH
  Resolution: setup_resolve() fetches 1d/5d forward returns, marks resolved=1 after 5 days.
  Endpoint: GET /api/setup-stats → breakdown by source/verdict/beta_blocked with win rates.
  Do NOT narrow the logged set — causes selection bias in /api/setup-stats.

SQLITE HARDENING (Jun 7 2026):
  All DB connections: timeout=30. WAL mode on the two high-write logs ONLY.
  setup_log.db and falling_knife_log.db: PRAGMA journal_mode=WAL in *_db_init(). The read-mostly
  cache DBs (scanner_cache.db, intraday_cache.db) intentionally do NOT use WAL — single writer.

PENDING (deferred — data-gated):
  Volume Leaders "Gates at a Glance" summary box — NOT added yet.
  Waiting for 50+ resolved signals in setup_log.db before writing summary with real numbers.
  When ready: SELECT source, verdict, beta_blocked, COUNT(*), AVG(ret_5d)
              FROM setup_log WHERE resolved=1 GROUP BY source, verdict, beta_blocked;

GIT STATE (Jun 7 2026): All committed — commit 9fe3557.
  local ~/Desktop/Stock-predictor/ = server /home/elimaoz99/stock_predictor/ = GitHub main.


JUN 14 2026 — LIVE TRACKER HARDENING (NOT committed to git yet)
=================================================================

THREE BUG FIXES in this session. All are in effect on server and local Mac.

BUG 1 — setup_resolve() used calendar days instead of data availability (db.py)
  Was:   resolved = 1 if (today - event_date).days >= 5 else 0
  Fixed: resolved = 1 if c5 is not None else 0
  Why:   6 calendar days != 5 trading days over a weekend. The old condition would mark
         resolved=1 with ret_5d=NULL on Saturday/Sunday. Fix: data presence drives resolution.
  Impact: setup_log.db had 53 events from Jun 8-12, all stuck at resolved=0.
          Will auto-resolve on Jun 16 (Mon) when the 5th trading day close becomes available.

BUG 2 — live_tracker.py sent no auth headers → HTTP 401 on all /api/scan calls since Jun 6
  Root cause: Basic Auth was added to api.py in May 2026 security hardening, but
              live_tracker.py (local script at the time) was never updated.
  Fix: added _basic_auth_header() in live_tracker.py — reads BASIC_AUTH_USERS from env,
       builds "Basic <base64>" header, injects into fetch_scan() requests.
  Impact: NO signals were logged in tracker.db from Jun 6-13 (9 trading days lost).
          Jun 3-5 signals were logged correctly (cron ran before auth was added).
  RULE ADDED TO CLAUDE.md: any api.py auth or endpoint change must also update live_tracker.py.

BUG 3 — ENV loading picked .env instead of api_data.env on server
  Was:   next((f for f in [".env", "api_data.env"] if exists)) — picks .env first.
         .env exists on server but does NOT have BASIC_AUTH_USERS → auth fix was silently ignored.
  Fixed: load BOTH files in order (api_data.env first, then .env) using os.environ.setdefault().
  Added: Fail-Fast warning to stderr at startup if ENABLE_AUTH=true but BASIC_AUTH_USERS not set.

LIVE TRACKER — NOW A SERVER CRON (cron was already set up, file was missing until today)
  Cron: 5 20 * * 1-5 cd /home/elimaoz99/stock_predictor && venv/bin/python3 live_tracker.py --log --no-telegram
  Time: 20:05 UTC (4:05 PM ET) Mon-Fri — after market close
  Log:  /home/elimaoz99/stock_predictor/tracker_cron.log
  DB:   /home/elimaoz99/stock_predictor/tracker.db  (was Mac-only until today — now server)
  Note: --no-telegram suppresses Telegram digest from cron. Run manually with --log to get digest.

TRACKER.DB STATE (Jun 14 2026):
  26 signals total:
    May 13 (4): APP, COIN, HOOD, PLTR — logged before regime/beta features, all high-beta
    Jun 3  (7): APP, HOOD, IBKR, PLTR, SHOP, SMCI, STX
    Jun 4  (5): APP, HOOD, PLTR, SMCI, STX
    Jun 5 (10): ANET, APH, APP, AVGO, HOOD, PLTR, SHOP, SMCI, STX, WDC
    Jun 14 (7): logged today with auth fix in place (symbols TBD — check tracker_cron.log)
  4 resolved (May 13 only): APP ✅ +0.16%, COIN ❌ -0.16%, HOOD ❌ -0.03%, PLTR ❌ -0.03%
    Hit rate: 25% (1/4). NOTE: all 4 were pre-beta-gate. COIN/HOOD/PLTR are high-beta → gate
    would block them today. Validates beta gate hypothesis.
  22 pending (Jun 3-5): ALL have beta > 1.4. live_tracker.py calls /api/scan (no beta gate),
    so these pass through even though Volume Leaders would block them. Separate code paths.
    → KNOWN MEASUREMENT BIAS / SHADOW TEST: tracker.db precision will read LOWER than the UI
      (UI suppresses these via the beta gate; /api/scan does not). Do NOT compare the two numbers
      directly until the code paths are reconciled. If these high-beta names resolve as losers
      (~Jun 17-18), that is the empirical green light to add the beta gate into /api/scan too.
  CAUTION before scp-ing tracker.db to server: CHECK server row count first.
    Jun 4-5 data was lost when Mac DB (11 rows) was uploaded over server DB (22 rows).
    Restored manually from tracker_cron.log. Always verify: python3 -c "import sqlite3;
    print(sqlite3.connect('tracker.db').execute('SELECT COUNT(*) FROM signals').fetchone())"

SETUP_LOG.DB STATE (Jun 14 2026):
  53 events from Jun 8-12, 0 resolved (Jun 13 = Saturday → only 4 trading days elapsed for Jun 8)
  Breakdown: OVEREXTENDED×18, OVERHEAD WALL×19, FADE RISK×11, VOL BREAKOUT×2,
             HIGH-BETA×1, DEVELOPING×2
  ZERO BUY signals in any scanner Jun 8-12 — all gates blocked everything.
  Resolution expected: Jun 16 Mon after 20:05 UTC (next cron run).

NEXT MILESTONE:
  Jun 16 after 20:05 UTC — first ~22 resolved outcomes from tracker.db (Jun 3-5 signals).
  Jun 28 — Jun 14 signals resolve.
  DO NOT build optimize_gates.py until ~50 resolved outcomes (~mid-July).
  The SQL to analyze gates is trivial; the data is not yet there.

GIT STATE (Jun 14 2026): NOT committed.   [SUPERSEDED — see JUN 15-18 section below: all committed + pushed]
  live_tracker.py changes (auth fix, env loading, fail-fast) are deployed but not pushed.
  db.py change (setup_resolve fix) is deployed but not pushed.
  CLAUDE.md (auth rule) is local only.


JUN 15-18 2026 — PHASE 0/1 SHIPPED + RESOLUTION-PIPELINE CHECKS
================================================================

SHIPPED & PUSHED (origin/main, latest commit 7ff08e8):
  test_scanners.py  46 unittest cases (zero deps) — locks the precedence ladders of
                    compute_verdict / compute_momentum / gainers_verdict (epsilon
                    boundaries, null-defenses, precedence traps). Phase 0 [DONE].
  deploy.sh         Verified deploy pipeline: unittest gate -> unified scp(3 files)
                    -> restart -> health curl (200|401, sleep+retry for cold-start)
                    -> frontend disk-hash check. --backend-only opt-out. Hang-proof
                    SSH (ConnectTimeout=4 + BatchMode=yes + curl --max-time). Verified
                    end-to-end in prod. CANONICAL deploy now (manual scp = fallback).
  ROADMAP.md        Acceptance-criteria roadmap, Phases 0-5 + a Phase 2 backlog.
  .gitignore        Protects .env / *.db / caches (prevented an .env leak on Jun 14).

RESOLUTION PIPELINE STATUS (Jun 17-18 read-only checks):
  tracker.db = HEALTHY. Jun 3 batch resolved correctly on the Jun 17 cron (10 full
    trading days, exit=Jun 17 close). Cross-validated vs an independent manual
    yfinance compute: same hit/miss direction on all 7. The "10-day lag" was NOT a
    bug — just pending the next run; resolver counts trading days as intended.
    Jun 4 resolves next run, Jun 5 the run after (staggered, not all 22 at once).
  setup_resolve() = BROKEN. setup_log stuck at 0/151 resolved. Three compounding
    issues: (1) LAZY — runs ONLY when /api/setup-stats is hit (db.py:296; no cron);
    (2) BURST — when triggered it fires ~151 rapid sequential yf.download -> Yahoo
    rate-limits the server IP -> all empty/raise; (3) SILENT-SKIP — except: pass
    swallows it and prints "Done" with 0 resolved. A single isolated download (GLXY)
    works fine, so data+logic are OK — the burst kills itself. Same disease as the
    FRED rate-limit. Fix queued in ROADMAP Phase 2: nightly incremental resolve cron
    + throttle/batch + log instead of pass. Gate-effectiveness data does NOT
    accumulate until this is fixed.

FIRST SHADOW TEST (Jun 3 batch, n=7, all beta>1.4) — COLLECTION ONLY, NOT CALIBRATION:
  3/7 hit, avg fwd_ret -3.95%. Tail-driven: SMCI -42.01% alone flips the cohort
  (other 6 avg +2.4%). Winners HOOD +23.3% / STX +16.6% / IBKR +8.2%; losers
  APP -18.7% / PLTR -11.3% / SHOP -3.8% / SMCI -42.0%. Confidence did NOT rank
  outcomes (HOOD 80% hit but APP 73% was 2nd-worst; IBKR 60% + STX 65% hit) — the
  model is overconfident on volatile names and does not price beta into confidence.
  Directional support for the beta gate (cuts the fat LEFT tail, not the win rate).
  n=7 is a statistical curiosity. NO threshold changes until 50+ resolved (~mid-July).

GIT STATE (Jun 18 2026): ALL committed AND pushed. origin/main = 7ff08e8.
  Working tree clean. Supersedes the Jun 14 "NOT committed" note above.


JUN 21 2026 (Sun) — DATA-POLLUTION AUDIT + TZ DATE HOTFIX + LEUMI TAB REMOVED
============================================================================

SHIPPED & PUSHED (origin/main):
  908012e  feat(frontend): remove Leumi Options tab. Friends finished using the
           calculator. Frontend-only (App.jsx): dropped tab button, render conditional,
           LeumiOptionsView + OptionsInput/OptionsStat + IL_BRACKETS/calcIncomeTax, and
           the now-unused Calculator lucide import. 9 tabs -> 8. No backend/DB/cron.
  30ade7d  fix(db): align logging dates with ET trading day using zoneinfo. SEE TZ BUG below.

JUNETEENTH (Fri Jun 19) DATA-POLLUTION AUDIT — crons fire by day-of-week (Mon-Fri) with NO
US-market-holiday guard, so they ran on the closed-market holiday on stale Thu data:
  tracker.db        4 phantom signals (DELL/GE/ANET/CCL, all high-beta) — live_tracker cron
                    ran Fri 20:05 UTC. DELETED.
  intraday_cache.db 10,920 rows under date 2026-06-19 (~double normal ~6,700) — fetch_intraday
                    cron ran Fri; a phantom trading day would have skewed the 20-trading-day
                    RVOL median in scanners.get_tod_rvol_cached. DELETED. (the dangerous one)
  falling_knife_log.db  clean (0 phantoms — FK logs 13-15 ET, nothing to catch on a closed day).
  Backups: *.bak-20260621 on server (consistent SQLite .backup() snapshots, taken BEFORE deletes).

PLOT TWIST — setup_log.db "Jun 19" rows were NOT holiday pollution but a TIMEZONE BUG:
  Forensics on the 25 rows: log_ts = 2026-06-18T21:00:48 UTC, one 0.2s burst (a single
  /api/gainers scan) — but the date column read 2026-06-19. Server TZ = Asia/Jerusalem (UTC+3):
  db.py stamped `date` via date.today() (Israel local) while log_ts used utcnow() (UTC). Any
  scan logged 21:00-24:00 UTC gets TOMORROW's Israel date. The 25 were a LEGIT Thu 17:00 ET
  gainers scan mis-dated to Jun 19 — likely an evening dup of the daytime Jun-18 gainers; the
  resolver is dead so zero impact. DELETED (recoverable from backup; kept deleted).
  -> Crons run before 21:00 UTC, so they are NOT TZ-affected — this is purely an endpoint-write bug.

TZ BUG FIX (commit 30ade7d; deploy.sh --backend-only; 46 tests green; HTTP 401 health OK):
  Added _signal_date() in db.py = datetime.now(ZoneInfo("America/New_York")).date().isoformat()
  (ET trading day, consistent with yfinance in setup_resolve). Applied at BOTH write sites:
  setup_log_event AND the identical latent pattern in fk_log_event (FK runs 13-15 ET — not yet
  triggered, same root). Cache (save_scan_results/get_latest_scan) + resolve-timing today()
  gates left untouched (out of scope). Verified on server: 21:00 UTC moment -> ET date 2026-06-18.

ROOT-OWNED DB FILES (infra finding):
  stock-app.service runs as User=root -> setup_log.db + falling_knife_log.db are root:root, so
  elimaoz99 over SSH can only READ them. BUT sudo NOPASSWD actually allows /bin/chown +
  /usr/bin/chattr (not just `restart`) — verified via `sudo -n -l`. So the fix is self-serviceable:
  `sudo -n chown elimaoz99:elimaoz99 setup_log.db` worked (corrects the old "restart ONLY" note).
  tracker.db / intraday_cache.db / scanner_cache.db are elimaoz99-owned.

STILL OPEN (unchanged today):
  #2 RESOLVER — setup_log still BROKEN, now 0/198 resolved. Same disease as Jun 18 (lazy +
     burst + silent except: pass). Now on stable ground (dates correct). HARD RULE: if any
     holiday/TZ phantoms ever exist again, delete them BEFORE reviving the resolver.
  #3 HOLIDAY GUARD — add a market-open check before Fri Jul 3 (observed Independence Day).
     Must sit in BOTH the cron scripts AND the endpoint logging path (gainers logging is
     endpoint-driven, not a documented cron).

NEXT MILESTONES:
  ~Jun 26-28 — tracker.db Jun 14-18 batch resolves (more beta-tagged shadow-test data).
               NOTE: Jun 3/4/5 are ALREADY resolved; nothing new lands before then.
  ~mid-July  — 50+ resolved; only then calibrate the beta gate / build optimize_gates.py.
  Before Jul 3 — land the holiday guard.

GIT STATE (Jun 21 2026): ALL committed AND pushed. origin/main = 30ade7d (before this handoff
  commit). Working tree clean apart from this handoff update.


JUN 25 2026 (Thu) — RESOLVER REVIVAL + ADJUSTMENT-BUG FENCE + HOLIDAY GUARD
==========================================================================

SHIPPED & PUSHED (origin/main):
  6e9ac8b  fix(resolver): revive setup_log resolver + fence both resolvers vs adjustment bug
  825c26f  feat(crons): US-market holiday/weekend guard across all DB-writing paths
  origin/main = 825c26f. Working tree clean apart from this handoff update.

#2 RESOLVER — REVIVED (closes the long-standing setup_log 0/N death):
  Was: 0 resolved out of 218 for weeks. Three confirmed causes in code: LAZY (ran only from
  /api/setup-stats), BURST (one yf.download per row x 200+ -> Yahoo 429), SILENT (except: pass
  printed success with 0 resolved).
  Fix (db.setup_resolve, max_rows=50): BOUNDED (WHERE resolved=0 AND date<=today-7d ORDER BY
  date LIMIT ?) + THROTTLED (time.sleep(0.5)) + per-row commit + real logging
  (candidates/attempted/resolved/failed). DECOUPLED from get_setup_breakdown (endpoint now
  read-only). New entrypoint resolve_setups.py -> cron `0 3 * * *` (03:00 UTC) drains ~50/night.
  First pulse (max_rows=15): candidates=15 resolved=15 failed=0 in 10.9s, no 429. Unresolved 218->203.

ADJUSTMENT-BUG FENCE (data-integrity, same commit 6e9ac8b) — BOTH resolvers, do NOT revert:
  Bug: return = adjusted_exit / RAW_entry. entry_price was the raw screener last_price stored at
  T=0; exit came from yf.download(auto_adjust=True). A split or ex-dividend INSIDE the window made
  numerator and denominator different bases -> fake return (split ~ -50% phantom; straddling ex-div
  ~ -div/price bias). Fix: use the ADJUSTED day-0 close as the denominator (db: closes[0];
  live_tracker: fetch_close_on(sym, logged_date)) — same auto_adjust basis as the exit, so
  corporate actions cancel. Raw price kept for reference only.
  EXPOSURE CHECK (read-only, run from the Mac to protect the server IP): tracker.db Jun cohort
  (22 symbols) had 0 splits and 7 ex-dividends, but EVERY ex-div fell OUTSIDE its resolution window
  (before entry or after exit) -> cohort NOT actively poisoned; the Jun-28 decision rests on clean
  data. fk_resolve_yesterday has the SAME latent pattern — NOT yet fixed (follow-up). STX ~$1054 /
  WDC ~$575 entry_prices verified REAL (AI-storage run-up), not corruption.

#3 HOLIDAY GUARD — DONE (commit 825c26f), closes the Juneteenth pollution vector before Jul 3:
  New standalone market_calendar.py (stdlib only): is_us_market_session(d=None), ANCHORED TO ET
  (datetime.now(America/New_York)) to match db._signal_date() — avoids the Asia/Jerusalem rollover.
  Weekend + hardcoded 2026 NYSE FULL-DAY holidays (half-days 11-27 / 12-24 excluded = market open).
  Injected at 3 points: db.setup_log_event (THE choke point — the only guard for the */25
  volume-leaders warm cron + open browsers hitting the API on a holiday; covers VL/gainers/reversion),
  live_tracker --log (skips signal logging; resolve still runs), fetch_intraday.main.
  resolve_setups.py deliberately NOT guarded (resolver on real historical bars -> holiday-agnostic).
  Verified on server: Jul-3-2026 -> False, today -> True; all crons import clean (no ImportError).
  WARN: NYSE list covers 2026 only — extend NYSE_HOLIDAYS_2026 + _COVERED_YEARS for 2027 (helper
  prints a stderr warning on uncovered years; weekend filter still applies).

DIAGNOSED, NO CODE SHIPPED:
  "Scanner shows only ALMOST BUY, no BUY" — market-driven, NOT a bug. Scanner runs light mode and
  labels signal=BUY from 0.57; the UI shows a REAL BUY only >=0.70 (the 0.57-0.70 band = ALMOST BUY).
  Real BUY (>=0.70) was abundant May->Jun 16 (max conf 0.78-0.88); drought began exactly Jun 17
  (max conf stuck 0.64-0.69). Precedent May 14-15 self-resolved Jun 16. Do NOT lower 0.70.
  Corrected a stale note: tracker.db logs the full BUY-tier (>=0.57), NOT >=0.70.
  v_accel column empty in Momentum Hunter — by-design timing: needs >=15 five-min bars = 75 min
  after open (first ~10:45 ET = 17:45 Israel). Israel users watch before it is ready; silent-None
  also collapses gainers verdicts to WATCH for the first 75 min. Honest fix (warming-up state +
  period="2d" today-filter + log) deferred to Phase 2.

OPS:
  launchd job on the Mac (com.elim.stockcheck.20260628) scheduled Sun Jun 28 08:00 Israel —
  read-only SSH check of whether the Jun 14-18 cohort resolved + whether the resolver is alive;
  self-removes after one run.

STILL OPEN:
  v_accel UX (silent-None) — Phase 2.
  User=root for stock-app.service — Phase 2 hardening (a web-app RCE = instant root on the VM).
  fk_resolve_yesterday — same adjustment twin as the two main resolvers; follow-up.
  Extend NYSE_HOLIDAYS_2026 for 2027 before year-end.

NEXT MILESTONES:
  Tonight  — resolve_setups 03:00 UTC drains ~50; live_tracker 20:05 UTC runs with the fence +
             holiday guard active.
  ~Jun 28  — tracker.db Jun 14-18 batch resolves (beta-tagged) -> beta-gate shadow-test decision,
             now on adjustment-clean data. launchd check fires that morning.
  ~mid-July — 50+ resolved; only then calibrate the beta gate / build optimize_gates.py.
  Jul 10   — VM downgrade e2-standard-2 -> e2-medium (GCP credits expire).

GIT STATE (Jun 25 2026): ALL committed AND pushed. origin/main = 825c26f. Working tree clean
  apart from this handoff update.


JUN 26 2026 (Fri) — VOLUME-LEADERS CACHE-WARM CRON 401 (3rd May-2026 auth victim)
================================================================================
  913124b  fix(cron): authenticate the volume-leaders cache-warm call.
  The */25 cron called /api/volume-leaders with NO credentials, so after the May-2026
  Basic Auth hardening it silently returned 401 every 25 min for weeks (output -> /dev/null).
  Effect: the backend cache only stayed warm while a browser had the tab open; on first
  mount you got a frozen cache (seen: 17:05 stale at 17:31). Same root cause as the Jun-14
  live_tracker 401. New warm_volume_cache.sh sends the first BASIC_AUTH_USERS pair (mirrors
  live_tracker._basic_auth_header) + logs the HTTP code to volume_leaders_cron.log; server
  crontab swapped to call it. Verified: manual run -> HTTP 200, fetched_at jumped to call
  time (cache breathing on its own again).
  TAKEAWAY: 3rd silent failure buried in /dev/null / unwatched logs (live_tracker, setup
  resolver, this). A FRESHNESS HEARTBEAT monitor (alert on stale outputs, not exceptions —
  the worst ones were silent no-ops, not crashes) is the #1 Phase-2 task after Jun 28.

GIT STATE (Jun 26 2026): ALL committed AND pushed. origin/main = 913124b. Working tree clean
  apart from this handoff update.


JUL 3 2026 (Fri, NYSE holiday) — FULL CODE REVIEW: JUN-7 REFACTOR REGRESSION + 5 BUG FIXES + N>=50 ANALYSES
===========================================================================================================
THE BIG FINDING — the Jun 7 refactor (97fef77+9fe3557) was built from a STALE api.py and silently
wiped 6 deployed-never-committed May features from production. Triple-verified: git log --all -S
"hod_gap_ratio" = empty (never in ANY commit), server md5 == local git (prod lost them too),
grep on live server = 0 hits. Lost: HOD Gap/ATR gate, RVOL slope gate, Power Hour whale alert,
merger-pinned filter, FALLING KNIFE logging call (fk_events frozen at 1 row since Jun 3),
wedge compression thresholds (scanners.py regressed to 0.15/0.20/0.25 vs the May-15 0.40/0.45/0.50),
plus market_id whitelist / top_n / min_confidence clamps from the May hardening. App.jsx still
rendered the dead fields (invisible, harmless). CLAUDE.md documented all of it as live = doc/code
drift for ~4 weeks. Root enabler: pre_scan.py-style "server is authoritative, git is stale".

SHIPPED (all deployed via deploy.sh + verified):
  34722c3  chore(pre_scan): imported the authoritative 342-line server copy into git — closes the
           exact deployed-not-committed gap that caused the whole incident.
  4667b20  fix: five correctness bugs found in the same review:
           (1) get_volume_leaders beta block used `closes` OUTSIDE the try that defines it —
               stale leak from the previous symbol = beta computed against the WRONG stock
               (or NameError->500 on first-symbol failure);
           (2) VIX state machine counted CALLS not days — days++ on every strategic-context
               cache miss, so min_days=5 graduated in ~2.5h of traffic; now ticks at most once
               per ET trading day (count_date in macro_state.json);
           (3) Basic Auth failed OPEN when BASIC_AUTH_USERS empty ("no_users_configured" let
               everyone in) — now 503 + startup stderr warning (env-load failure = loud, not public);
           (4) scanner_cache.db connections had no timeout=30 (violated own rule);
           (5) live_tracker busday_count/offset ignored NYSE holidays — holiday weeks resolved
               outcomes one trading day early; now passes NYSE_HOLIDAYS_2026.
  CLAUDE.md: "OUT OF SERVICE" banner + NOT-IN-CODE tags on every lost-feature section (specs kept
  as re-implementation source — no git source exists).

ANALYSES RUN (both pre-registered thresholds crossed):
  tracker.db locked query (unchanged, anti-HARKing): N=34 resolved-with-beta -> COLLECTION ONLY.
  high(b>1.5) n=21 hit 47.6% median +2.55% vs normal n=13 hit 30.8% median -1.24% — direction
  UNCHANGED from Jun-30 (weak COUNTER-gate evidence). Huge caveat: repeated names dominate
  (HOOD x6 all wins, DLTR x3, HWM x5) with overlapping windows -> effective N << 34. Decision at
  N>=50 (~mid-July).
  setup_log first breakdown (261 resolved, Jun 8-25): OVERHEAD WALL n=105 med -3.63% win 35%,
  FADE RISK n=85 med -2.03% win 35% -> the protective blocking VALIDATED directionally.
  DEVELOPING n=11 mean -8.71% win 18% -> WORST bucket (red flag, no action, n small).
  BREAKOUT CONFIRMED n=7 mean +3.15% win 86% -> the only positive verdict IS positive.
  KEY LIMIT: zero data on the LOST gates (setup_blocked_by never logged) -> restoration cannot be
  data-driven; restore spec-faithful WITH logging so the NEXT ~50 rows measure them.

PLANNING: restoration spec v2 locked with 5 user-driven upgrades — (a) evaluate ALL gates, log
  blocked_reasons as JSON array (no short-circuit credit-stealing); (b) market_state+vix_state
  columns at signal time; (c) intraday archive-before-purge (cold storage); (d) EARLY_CLOSES
  half-day awareness (Nov 27 / Dec 24 close 13:00 ET); (e) DEVELOPING display breaker
  PRE-REGISTERED as a safety rule (N>=20 & mean<-5% -> demote display to WATCH, log unchanged).
  VM downgrade readiness verified (service enabled, disk ok, static IP) and MOVED UP to Sun Jul 5 —
  remaining credit days = free canary window with free rollback.

JUL 5 2026 (Sun) — POST-HOLIDAY GUARD PASS + FULL RESTORATION SHIPPED + VM DOWNGRADED
=====================================================================================
HOLIDAY GUARD FIELD TEST (first live fire, Jul-3 Independence Day): PASS on all fronts. Zero
  Jul-3 rows in all three DBs; both cron logs show the guard "skipping" lines from Fri evening;
  Thursday data intact; setup_log choke held through a full weekend of */25 warm-cron force=true.
  Juneteenth incident class formally CLOSED.
SHIPPED:
  (pre-restore) feat(warm-cron): gated to 09:00-16:59 ET trading days via market_calendar —
  was forcing the full VL pipeline (screener+yfinance+on-demand ML) every 25 min 24/7.
  7422e25  feat(restore): ALL 6 lost features re-implemented from spec + whitelist/clamps
  (400/422 verified live) + the full spec-v2 instrumentation. Field names kept identical ->
  App.jsx lit up with ZERO frontend changes. fetch_intraday: archive-before-purge (csv.gz) +
  actives_seen universe purge TRACK_DAYS=10 (ends the monotonic 157-symbol growth). Tests 46->52
  incl. a SYNTHETIC-WEDGE regression tripwire (a 0.30-compression wedge must NOT detect; 0.65 must)
  so the threshold regression can never silently return. Deploy note: market_calendar.py,
  fetch_intraday.py, models.py are NOT in deploy.sh's file list — scp them manually when touched.
VM DOWNGRADE (user in Console, Claude verifying): e2-standard-2 -> e2-medium. Service auto-started
  (enabled bet paid off — first reboot in 75 days), health 200, crontab intact, DBs intact,
  static IP held. SWAP: deliberately NOT created — user challenged the need; rest-state math
  (745MB used / 3.16GB avail vs 1.2-1.5GB yfinance spikes) says fine; decision = wait for
  live-load memory.peak data instead of pre-provisioning. Data-driven infra, same discipline.

JUL 7 2026 (Tue) — DAY-1 CANARY HELD + PRE-OPEN LOGGING BUG (QNT) + ANALYSIS SELF-AUDIT + DROUGHT RE-TRIAGE
===========================================================================================================
DAY-1 CANARY (Jul 6 = first live trading day on e2-medium WITH restored features): HELD.
  Zero OOM, NRestarts=0, 24x HTTP 200 warm hits on the heaviest endpoint during the session
  (+48 off-hours skips = gate working). Benign 06:46 IDT restart (likely unattended-upgrades)
  reset memory.peak -> session peak unknown, recapture pending.
INSTRUMENTATION LIVE: all 62 Jul-6 setup_log rows carry market_state+vix_state; restored gates
  fired AND recorded — INTC ["RVOL"], TSLA ["RVOL"], GRAB ["HOD"] where GRAB is a HOLD row the
  old coverage would have dropped (coverage-expansion proven). N>=50 calibration clock started.
PRE-OPEN STALE-LOGGING BUG (user-surfaced by asking about QNT): at 00:22 ET the gainers endpoint
  served Yahoo's stale Jul-6 closes (is_live=False, +11.55% = YESTERDAY'S move) while
  db._signal_date() had already rolled to Jul-7 -> 25 of yesterday's signals re-logged as today's.
  Double harm: non-independent duplicates in the N>=50 pool AND UNIQUE(source,symbol,date) would
  BLOCK the real intraday rows at the open. The holiday guard passes (Jul-7 IS a trading day) —
  it checked the DATE, not whether the SESSION opened. 4th member of the date/session bug family
  (TZ rollover, holiday, weekend, now pre-open).
  d82f6fa  fix(db): market_calendar.has_session_opened() — loggable from 09:30 ET -> midnight ET
  on trading days only (post-close stays loggable: quotes ARE today's; dedup handles repeats).
  Guards BOTH setup_log_event and fk_log_event choke points. Cleanup: .backup() snapshot
  setup_log.db.bak-20260707 then surgical delete of the 25 rows (log_ts < 13:30 UTC only).
  Live-verified pre-open: guard False + real write attempt blocked. Tests 52->58.
ANALYSIS SELF-AUDIT (user asked "was our analysis ideal?" — it wasn't):
  Flaw 1 (substantive): used CLOSE-based ret_5d but a signal-follower enters at SIGNAL price —
  same execution-slippage bias already documented for FK and NOT applied. On signal-price basis
  QNT +8.27%->+4.99%, RCUS flipped win->LOSS (-1.62%); the "no vwap_gap gate" conclusion SURVIVED
  (extended still beat non-extended, r +0.66->+0.43) — but by luck, not method.
  Flaw 2: QNT circularity — the hypothesis-generating observation dominated the n=7 test set.
  Flaw 3: post-hoc bucket boundaries. Clean: TZ-residue audit found 0 mismatched rows in 434.
  NEW STANDARDS: dual-basis (close + signal-price) in every setup_log analysis; H1 (vol_ratio<1.5
  inside BREAKOUT CONFIRMED not harmful) + H2 (vwap_gap>3% not harmful) PRE-REGISTERED for the
  N>=50 confirmatory test — buckets <=1/1-3/>3 fixed, median signal-price ret_5d net of 0.16%
  commission, min n=15/bucket, QNT Jun-18 rows EXCLUDED. The truly tradable question (VWAP-bounce
  entry outcomes) is NOT answerable from setup_log -> Retrospective Pipeline territory.
  Exploratory findings (NO code changes): vol_ratio-low inside BREAKOUT beat vol_ratio-high
  (opposite of intuition); vwap_gap Simpson split — harmful across all-gainers n=211 (win 42/35/26%
  monotonic) but the pool is 97% avoid-verdicts; within BREAKOUT n=7 the extended ones ran hardest.
  Intraday fade != 5d outcome (QNT faded both appearances, recovered +8.27% the first time) ->
  REINFORCES "signal=awareness, enter on VWAP bounce" rather than any new gate.
BUY DROUGHT RE-TRIAGED (user: "no high-conf BUY for a while — is the system OK?"): system healthy,
  strongest evidence yet it's by-design — 478-491 models/day all through, and the gate OPENED
  exactly once mid-drought: Jun-26, 3 REAL BUYs max conf 0.794, 2-3 sessions after the Jun-23
  -3.29% Nasdaq selloff. Mean-reversion model bought the dip, silent at the highs (SPY +3.1%/5d,
  VIX 15.6; top HOLD confidences 0.80-0.84 = confidence exists, attached to HOLD). Tracker's live
  0.57-0.70 band (~41% hit on 34) reinforces NOT lowering 0.70. Momentum tabs are the rally tool.

STILL OPEN:
  Verify real Jul-7 rows DO log after today's 16:30 IDT open (the guard must not block truth).
  Day-2 follow-through on the 8 Jul-6 signals after tonight's close.
  Recapture service memory.peak under full market load (reset by the benign restart).
  FK pipeline end-to-end still unproven — fk_events has 1 row; needs a human hit on
  /api/reversion-leaders inside 13:00-15:00 ET (no warmer exists for it).
  VM Manager + Network Intelligence Center — disable in Console if not yet done.
  v_accel UX / User=root / 2027 NYSE holidays — unchanged from Jun 25 list.

NEXT MILESTONES:
  Jul 6-9   — e2-medium canary window (free rollback until credits expire Jul 10).
  ~mid-July — tracker N>=50 -> beta-gate decision (locked query, unchanged);
              instrumented-gate rows accumulating toward their own N>=50 calibration.
  Nov 27 / Dec 24 — first half-day sessions exercising session_close_hour().

GIT STATE (Jul 7 2026): ALL committed AND pushed. origin/main = d82f6fa. Working tree clean
  apart from this handoff update.


JUL 7 2026 (EVE) — LEVERAGED SENTIMENT STRIP SHIPPED (OBSERVATIONAL ONLY)
==========================================================================
  d2d318a  feat(sentiment): SOXS:SOXL + SQQQ:TQQQ DOLLAR-volume short/long ratios as market
  sentiment. Born from the ETF-tab request — the most-active ETF list is ~90% leveraged mirrors
  of already-scanned stocks, so NO new tab; the ratio rides get_market_context() as
  lev:{semis,qqq} (no new endpoint, no cron), logged RAW per signal (setup_log.lev_sent_semis /
  lev_sent_qqq, idempotent migrations, all 3 call sites) + neutral gray strip in the VL bar.
  DESIGN-CHANGING DISCOVERY during the live smoke test: baseline is ~0.3 NOT 1.0 — long
  leveraged ETFs carry far larger DOLLAR volume (SOXL $11.4B vs SOXS $3.2B on a heavy red day);
  the "SOXS 9:1 fear" share-volume read was a unit-price artifact, dollar basis showed 0.28 =
  dip-buying. Consequences: NO Fear/Greed labels in v1; labels + bucket boundaries re-registered
  from ~2 weeks of logged distribution BEFORE any outcome unblinding. Display + collect only —
  promotion to any gate requires the pre-registered N>=50 test.

JUL 9 2026 — RESOLVER STARVATION FIX
=====================================
  d518ed8  fix: setup_resolve's own 10-day lookback window starved the backlog — rows born in
  the resolver's dead period aged past the window and could never resolve. Window widened to
  16d + a resolved=-1 tombstone for rows >30d old (kills zombie retries) + "stale=" counter in
  the nightly log line. 33 stuck rows drained live at deploy; verified on the Jul-10 cron.

JUL 10 2026 (Fri) — FK VERIFY CLOSED + ROOT-DB WRITE BUG FIXED + SITTING SPEC LOCKED
=====================================================================================
CLOSES three "STILL OPEN" items from the Jul 7 entry:
  Jul-7 rows logged post-open: YES — 29 clean rows after the d82f6fa guard (bug fix proven).
  Day-2 follow-through on the 8 Jul-6 signals: captured — ALL 8 deepened, mean -6.9% vs QQQ
  -2.1% (awareness-not-entry vindicated; final read at ret_5d).
  memory.peak recaptured: 320MB full-session peak on a heavy red day -> e2-medium downgrade
  PROVEN, swap deliberately not created. Canary closed, credits deadline passed safely.

FK PIPELINE END-TO-END — VERIFIED AND CLOSED (the last Jul-7 open item):
  Root cause of fk_events silence was COVERAGE, not code: zero human/warm traffic ever hit
  /api/reversion-leaders inside the 13:00-15:00 ET logging window since the Jul-5 restoration.
  Self-removing FKVERIFY-TEMP crontab curls (20:10/20:50/21:30 IL = 13:10-14:30 ET, JSON bodies
  saved to /tmp so "no candidates" is distinguishable from "bug") fired 3/3 HTTP 200.
  Zero fk_events rows = CORRECT-BY-SPEC: IONS (the live candidate, -23.9% in the morning) had
  recovered to -8.3% and based — RSI 32.6 (oversold) but vwap_gap only -0.67% (needs <-2.0) and
  ml HOLD -> verdict WATCH, and WATCH never downgrades to FALLING KNIFE. Gap-down-then-base is
  by design NOT a knife. Crontab self-cleanup verified (0 lines left) + /tmp wiped.
  THE BONUS BUG (found by a synthetic write test, would have bitten months later):
  falling_knife_log.db was still root:root 644 -> fk_log_event from user context failed with
  "attempt to write a readonly database", SWALLOWED by the bare except. The API service (root)
  could write, but the 03:00 resolver (runs as elimaoz99, reaches fk_resolve_yesterday via
  db.py) could NEVER have marked future FK rows resolved — silent forever-stuck rows. Fixed:
  sudo -n chown to elimaoz99 (NOPASSWD covers chown — header section corrected accordingly),
  synthetic insert->verify->delete test green, table left clean (1 row = CRCL Jun-3).
  ALL log DBs now elimaoz99-owned. User=root on the service itself remains Phase 2.

EXTERNAL EXIT-ANALYSIS REJECTED (methodology defense in action): an outside-agent script ran
  a 9-cell SL/TP grid over 67 resolved tracker rows and concluded "-15% disaster stop is
  optimal (+3.41% vs +2.77%)". Verification killed it: 48/67 rows (72%) are sub-gate
  (conf 0.57-0.70, never traded) including ALL 3 SMCI rows that supply the catastrophe; 5 of the 6
  real-BUY -15% breaches are ONE early-June episode (APP x3 consecutive-day entries, PLTR, WDC)
  -> ~2 independent events, uncalibratable (same anti-HARKing bar as the fk_events >=30 rule);
  WDC Jun-5 hit -16.4% MAE and +39.0% MFE in the SAME window — the proposed stop sits mid-noise
  and plausibly kills the system's best runner. What SURVIVED: tight take-profits destroy the
  right tail (TP=5% -> avg +0.08%/trade) — consistent with the fixed-horizon design, which has
  NO exit geometry and is structurally immune to disposition/geometry artifacts. Script backed
  up off-server and deleted from the prod dir. MAE/MFE outcome columns parked as a possible
  post-milestone, pre-registered addition.

SITTING SPEC LOCKED (the ~Jul-21 milestone bundle, all recorded in memory):
  1. Locked shadow query re-runs UNCHANGED (3 pre-committed decision rules).
  2. Payoff-ratio addendum (dual-basis, medians alongside means).
  3. Bootstrap CI addendum — CLUSTER bootstrap: resample whole TRADING DAYS not rows (rows
     cluster ~45-49/day and co-move; the June episode above is the live proof), B=10,000,
     seed=42, median ret_5d, 95% CI + P10 "sane worst case". INTERPRETIVE ONLY.
  4. Scanner-health criterion locked at the sitting (ops task, anchored on Jun-23->26).
  5. CLOSED CONFIRMATORY FAMILY — exactly 4 tracks may drive code change: shadow query, H1,
     H2, lev_sent buckets. Everything else run at the sitting is descriptive, no matter how
     significant it looks; at most it seeds the next pre-registered question.
  Parked post-milestone: overnight-gap covariate (log-only path), MAE/MFE outcome columns.
  NEW HARD RULE (memory + here): setup_log is FORWARD-ONLY — never backfill feature values
  into rows older than the feature's deploy date; new columns start NULL and fill forward.
  CONFOUNDER recorded for the lev re-registration: a visible retail wave of agentic bots
  (broker-productized) trades TQQQ/SQQQ momentum mechanically, skewing SQQQ dollar-volume UP
  on red days exactly when a fear read matters -> lev_sent_semis (less bot-crowded) becomes
  the PRIMARY covariate, qqq secondary.

JUL 12 2026 (Sun) — N>=50 CROSSED + WATCHDOG INSTALLED + SITTING MOVED TO JUL 24+
==================================================================================
RESOLVER BACKLOG DRAINED: Jul-11/12 crons resolved 49+18 with stale=0 failed=0; the Jul-2 x68
  batch is gone (one straggler each from Jul-1/Jul-2 still retrying nightly; 30-day tombstone
  is the backstop). Jul-6 x62 resolves on the Jul-13/14 runs.
SHADOW POOL CROSSED N>=50: resolved-with-beta = 52. NO PEEKING — the sitting trigger is BOTH
  legs (N>=50 AND ~2wk lev distribution), and the confirmatory family runs ONLY at the sitting.
LEV FILL 3-for-3: Jul-8 45/45, Jul-9 49/49, Jul-10 38/38 rows at 100% lev_sent fill
  (semis 0.25-0.37, qqq 0.46-0.52 — baseline confirmed).
WATCH ITEM: resolved BREAKOUT CONFIRMED = 22; H1/H2 need n>=15 per bucket — if buckets are
  thin at the sitting, H1/H2 defer to a later checkpoint (definitions do NOT loosen).
WATCHDOG (finally closes the Jun-26 TAKEAWAY "freshness heartbeat = #1 Phase-2 task" — all
  three silent failures were no-ops, not crashes):
  watchdog.py + cron "30 9 * * *" (# WATCHDOG-DAILY) -> ONE Telegram digest/day on the existing
  pre_scan channel (creds from api_data.env). Read-only checks, each exception-isolated:
  API answering on :8000; resolver "done" within 26h + counters (failed>0 = red); yesterday's
  setup_log rows + 100% lev fill (trading days only, via market_calendar); disk <90%;
  MemAvailable >150MB. Green heartbeat daily BY DESIGN — sudden silence means the watchdog
  itself died. HARD RULE: alert-only, it never restarts/fixes/writes anything.
  Live-tested green + Telegram received. Imported into git the same day (no repeat of the
  pre_scan deployed-not-committed gap).
VACATION Jul 19-23 (user unavailable) -> THE SITTING MOVED TO JUL 24 OR LATER. Data keeps
  accumulating (pure upside for the thin buckets); code freeze absolute; watchdog covers.

STILL OPEN:
  Jul-1/Jul-2 straggler rows (1 each) — watch the nightly stale counter; tombstone at 30d.
  CLAUDE.md staleness sweep: Infrastructure still says e2-standard-2 + "downgrade planned"
  (done Jul 5, proven Jul 10); crons list missing resolver (03:00 IL) + watchdog (09:30 IL);
  pending-actions list has completed items. Sweep before the sitting.
  VM Manager + Network Intelligence Center — disable in Console if not yet done.
  v_accel UX / service User=root (Phase 2) / 2027 NYSE holidays — unchanged.

NEXT MILESTONES:
  Jul 13-14 — resolver drains the Jul-6 x62 batch.
  Jul 19-23 — vacation; watchdog on duty; no changes of any kind.
  Jul 24+   — THE SITTING (one sitting closes: locked query decision, H1/H2, lev bucket
              re-registration + label calibration, health-threshold locking, payoff +
              cluster-bootstrap addenda; the closed family caps what may drive change).
  Nov 2026  — restore MIN_PRECISION_BUY 0.30 -> 0.48 (regime-mix criterion, not calendar).
  Nov 27 / Dec 24 — first half-day sessions exercising session_close_hour().

GIT STATE (Jul 12 2026): origin/main = d518ed8 before this update; working tree clean apart
  from this handoff update + watchdog.py (imported from server, committed together).


JUL 14-16 2026 — SITTING TOOLING + SECTOR HEATMAP (the last two pre-freeze deploys)
===================================================================================
  65e32a1  lev_sitting.py — the BLINDED two-stage tool that makes the lev bucket decision
  anti-HARKing-proof rather than trust-based. Stage A reads covariates ONLY (per-day median
  of lev_sent -> median of day-medians -> bucket boundary), preview by default, `--freeze`
  writes lev_spec_frozen.json exactly once and REFUSES to overwrite. Stage B (`--unblind`)
  refuses to run without a frozen spec and counts eligible rows (resolved BREAKOUT CONFIRMED
  with lev + outcomes) BEFORE selecting any ret_* column, exiting below N=50 — so the N-guard
  cannot be dodged by peeking. `--selftest` plants a known effect in a synthetic DB and verifies
  boundary recovery + effect recovery + the N-guard. Pure stdlib, read-only DB access.
  Server preview Jul-14: 5 days / 172 rows, tentative semis 0.29 / qqq 0.47 — NOT frozen.
  TWO CLOCKS, do not conflate: the BOUNDARY freezes at the sitting; the OUTCOME test runs only
  when its own N>=50 matures.
  c752e4b  feat(context): 11-SPDR sector heatmap in the VL market-context bar. Deployed Jul 15 —
  deliberately the last day before the Jul 16-23 freeze. Same proven pattern as the lev strip:
  one batched yf.download of 11 fixed tickers, `_SECTOR_HEATMAP_TTL`=300s, whole fetch in
  try/except -> stale-or-None (a sector failure must never break a scan), pure math in
  `_compute_sector_changes()` unit-tested, rides inside get_market_context() as
  `sectors: {XLK: pct, ...}` — no new endpoint, no cron. v1 constraints honored: DISPLAY ONLY,
  NO setup_log columns, NO gate. Frontend = sorted CSS-grid strip, buckets at +-0.5%/+-1.5%.
  4 new tests (61 total).
  8e6c566  feat(context): SOXX industry overlay — COMMITTED Jul 16, deployed Jul 24 (freeze
  respected; staged in repo with the deploy deferral written into the commit message itself).
  Rationale proven live on commit day: XLK -2.64% while SOXX -5.23% — the sector tile dilutes
  semi moves ~2x on exactly the days that matter. SOXX is an INDUSTRY ETF inside XLK, not a GICS
  sector (tooltip says so); it pairs with the lev strip: SOXS:SOXL = leveraged flow, SOXX = price.
  Grid 11 -> 12 cols.

JUL 19-23 2026 — VACATION (user unavailable) + THE JUL 16-20 SELLOFF
====================================================================
Absolute code freeze held. Watchdog ran solo and stayed green every day — the Jul-12 investment
  paid for itself as designed: no human eyes needed, and a green heartbeat daily BY DESIGN so
  silence itself is the alarm.
The Jul 16-20 selloff produced a 3-trading-day run of ZERO actionable signals (no VOL BREAKOUT,
  no BREAKOUT CONFIRMED) — correct behavior, the reversion model staying out of a falling market.
  This became the live counterexample that killed the pre-registered drought criterion at the
  sitting (see Track 4 below). Drought broke Jul-23: ICE @ conf 0.76, the first real BUY in
  weeks, ~3 sessions after the selloff — the SAME dip-buying pattern as Jun-23 -> Jun-26.
  Resolves ~10 sessions out on its own clock, unrelated to the sitting.

JUL 24 2026 (Fri) — THE SITTING: ALL 4 TRACKS CLOSED, 3 OF THEM WITH ZERO CODE CHANGE
=====================================================================================
Gate check at open (user present, GO given): tracker resolved-with-beta = 78 (>=50 OK),
  BREAKOUT CONFIRMED resolved = 21 (>=15 nominally OK but thin), lev_sent resolved = 252,
  watchdog green through the whole vacation, mem peak ~340MB.

TRACK 1 — beta-gate shadow query: DECISION = DO NOT extend the gate into /api/scan. Status quo
  (keep the existing get_volume_leaders gate) + keep collecting. The locked rule was "if
  high-beta (>1.5) BUYs actually LOST -> green light to port the gate"; they did NOT clearly lose.
  Raw basis N=78 (high=40, normal=38): hit%(>=+3%) high 42.5 vs normal 28.9; mean% high -0.23 vs
  normal +1.35; median% high +1.25 vs normal -1.17; payoff high 0.70 vs normal 2.55. Reading:
  high-beta hits MORE often with a higher median but carries a FAT LEFT TAIL (the SMCI -42% class
  of event); normal misses more but wins bigger when right.
  Cluster bootstrap (days: high=18, normal=17; B=10k, seed=42): median 95% CI high=[-3.69,+4.51],
  normal=[-2.98,+0.04] — both straddle 0. Effective N is the ~18 DAYS, not the 78 rows.
  Inconclusive BY DESIGN, which is why the decision rule and not the eyeball decided.
  BASIS GAP FOUND (important, carries forward): tracker `outcomes` stores only RAW fwd_ret, and
  `hit = fwd_ret >= 0.03` (HIT_THRESHOLD, live_tracker.py:31) is thresholded-raw, NOT
  market-adjusted — so the dual-basis net-of-SPY leg NEVER RAN for this track (forward SPY over
  the holding window isn't stored; entry_spy_ret5d is trailing-at-entry only). Market-adjusted
  return is an OUTCOME, so computing it retroactively via yfinance is NOT a forward-only
  violation — deferred only because the raw basis is already noise-dominated and won't flip the
  verdict. TODO for a future checkpoint: have the resolver capture forward-SPY so the
  market-adjusted basis exists LIVE. Re-run trigger = more TRADING DAYS, not more rows.
  Payoff + bootstrap are EXPLORATORY-ONLY per the closed-family rule: the fat-tail observation
  may NOT drive action now, it may only seed a new pre-registered question.

TRACK 2 — restoration H1/H2: DECISION = DEFER BOTH. No code change. The pre-registered floor of
  n>=15 per bucket is not met: H1 (vol_ratio <1.5 vs >=1.5) buckets = 16/5; H2 (vwap_gap >3% vs
  <=3%) = 12/9. With only 21 resolved BREAKOUT CONFIRMED, ANY 2-way split forces a sub-15 bucket.
  Spec rule = thin buckets DEFER, definitions do NOT loosen.
  EXPLORATORY RED FLAG (may not drive action; seeds a NEW pre-registered question): all buckets
  show NEGATIVE median net-of-commission ret_5d (-3% to -5.4%) across all 21 rows — i.e. the
  BREAKOUT CONFIRMED verdict ITSELF looks unprofitable net, not merely a sub-condition of it.
  Candidate future question: does BREAKOUT CONFIRMED need a display breaker like DEVELOPING's?
  DO NOT act on this yet.
  REGIME-MIX CAVEAT reinforcing the defer: only 10 of the 21 rows carry market_state — 11 predate
  the Jul-5 restoration instrumentation, so the pool is a pre/post measurement-regime blend.
  Argues for a clean forward re-run over deciding on today's mixture.

TRACK 3 — lev_sent bucket re-registration: DONE, boundary FROZEN; outcome test DEFERS. No code or
  logic change (the freeze writes a pre-registration spec file, nothing else).
  `lev_sitting.py --freeze` (selftest green first), covariate-only and blind to outcomes:
  13 trading days / 362 rows -> median-of-day-medians boundary semis = 0.37 (PRIMARY),
  qqq = 0.47 -> lev_spec_frozen.json (2026-07-24T12:12Z, refuses overwrite; tie value<=boundary
  -> LOW, min_n=50, commission 0.16%, bootstrap B=10k seed=42, resample=trading_days). semis
  ranged 0.24-0.415 over the 13 days, so the boundary sits mid-distribution — a sensible split.
  THIS is the anti-HARKing lock: every future outcome test buckets against a boundary chosen
  without ever seeing a return.
  Stage B outcome test: eligible N = 3 / 50, and "no outcome columns were read". HONEST SCOPE
  CAVEAT: because the pre-registered test is restricted to BREAKOUT CONFIRMED (a rare verdict)
  and lev only collects since Jul-8, N=50 is likely MONTHS away, not weeks. That makes it a
  candidate for a future RE-REGISTRATION discussion (broader signal set?) — NOT changed now,
  which would be goal-moving.
  Follow-up now UNLOCKED but NOT done: calibrate the display strip's LOW/HIGH labels around
  semis 0.37 (frontend display-only; ~2.5wk of distribution now exists).

TRACK 4 — scanner-health threshold: DECISION = DEFER the pre-registered drought-vs-broken
  criterion because it is MIS-SPECIFIED, and ship a sound liveness check instead.
  Why it fails: "significant drop + zero BUY/actionable within 3-5 days => flag broken" would
  FALSE-POSITIVE. (a) zero-actionable days = 12/30 (40%) — normal, not anomalous; (b) a HEALTHY
  3-day zero-actionable run already happened Jul 16-20 in the real selloff, where staying out was
  correct — a window calibrated on the shallow Jun-23 -2.13% dip would have mis-flagged it;
  (c) only 1 of 31 days ever hit <= -2% spy_ret5d and ZERO hit <= -3%, so locking a drop threshold
  = calibrating on N=1. In a real selloff, "rows in, zero actionable out" is INDISTINGUISHABLE
  from over-gating without forward knowledge of the bottom. Re-approach after 2-3 real
  selloff+recovery episodes.
  7ff25c2  feat(watchdog): check #6 "cron output freshness" — the robust invariant instead:
  the three daily crons nothing else watches must have FRESH OUTPUT.
    tracker.db      MAX(date_logged) >= last US session   (live_tracker 20:05 Mon-Fri)
    intraday_cache  MAX(date)        >= last US session   (fetch_intraday 20:30 Mon-Fri)
    wedge_cache     scan_ts age      <= 26h               (pre_scan 05:00 daily)
  market_calendar handles weekends/holidays -> 0% market/calendar false positives.
  scanner_cache.db DELIBERATELY EXCLUDED (multiple writers, variable cadence = the naive-freshness
  trap). This recreates the detector that would have caught the Jun 6-14 live_tracker 9-day silent
  auth outage. Dry-run green + negative test fires red on a simulated stale output. scp'd live.

SITTING CLOSED. Discipline win: 3 of 4 tracks changed NO code — exactly what sound
  pre-registration is supposed to produce. Also deployed at the sitting: SOXX (8e6c566) via
  deploy.sh, 61 tests green, 12 tiles verified live in market_context.

JUL 26 2026 (Sun) — COLLECTION GAP: GAINERS + REVERSION HAD ZERO CRON COVERAGE
==============================================================================
  709bc52  fix(collection): warm gainers + reversion-leaders too, not just volume-leaders.
  User noticed a collection problem and first proposed a browser-automation skill to "open the
  site and collect data." Investigation found the cause was OPERATIONAL: warm_volume_cache.sh
  (the */25 cron) only ever force-refreshed /api/volume-leaders. gainers and reversion_hunter had
  ZERO cron coverage — they were scanned only when a real browser happened to open that tab.
  IMPACT: setup_log showed reversion_hunter = 0 rows EVER across the full 48 days since the Jun-8
  inception, and gainers collection had stopped dead after Jul-21. The reversion call site itself
  was verified correctly wired in code (NOT a repeat of the Jun-7 FK-logging-dropped incident) —
  the DEEP BUY / POTENTIAL BOUNCE condition is structurally strict, but with zero warmed samples
  it was impossible to distinguish "genuinely rare" from "never sampled."
  CONSEQUENCE for the Jul-24 Track 2 defer: the thin BREAKOUT CONFIRMED bucket may be partly an
  artifact of this same starvation rather than pure verdict rarity — re-run H1/H2 with fresh eyes
  after the new coverage has run a few weeks.
  FIX SHAPE (deliberate): extend the existing script, build nothing new — same Basic Auth +
  market_calendar gate + HTTP-code logging already proven for volume-leaders. ALTERNATES gainers
  and reversion-leaders one per run (state file warm_alt_state, flips 0/1 each tick) rather than
  warming both every cycle, to avoid doubling Yahoo call volume and e2-medium load on top of the
  unchanged volume-leaders warm. User chose the staggered option explicitly over "warm all 3
  every run" and "new separate cron." Verified: both endpoints 200 on force=true; the gate
  correctly skipped on the non-trading Sunday it was installed.
  WHY NOT the browser skill: every verdict is already computed server-side on each API call, so
  scraping rendered HTML would be a slower, more fragile duplicate with none of the existing
  safeguards. The fix was operational parity, not new architecture.
  ANALYSIS RULE THAT FOLLOWS: pre-Jul-26 gainers/reversion rows came from incidental browser
  traffic ONLY. Sample composition and density shift starting ~Jul 27. Never mix the pre/post
  eras without noting the regime shift — same discipline as the Jun-7 measurement-regime caveats.

JUL 28 2026 (Tue) — DAILY REVIEWS TAB REBUILT + DISK-CACHE PERSISTENCE FOR ALL 3 SCANNERS
=========================================================================================
  a070db2  Daily reviews tab: real dates, lazy loading, server-side search.
  THE DATE BUG: accordion titles showed raw filenames on 109 of 111 reviews. The parser expected
  exactly 3 underscore fields, but the files carry a 4th
  (stock_recommendations_27_07_2026_Evening.txt), so nearly every one fell through to the basename
  fallback. Now parses the optional session field, labels it in Hebrew, and sorts by PARSED DATE
  instead of mtime.
  THE PAYLOAD BUG: opening the tab shipped all ~111 reviews in full — 824KB — of which exactly one
  was ever read. /api/recommendations now returns metadata + a preview line (34KB), with full text
  served per review by /api/recommendations/{id} (regex + basename guards against path traversal).
  Search moved SERVER-side behind ?q= so it still spans every review, with an mtime-keyed content
  cache and a 300ms debounce in front of it.
  FRONTEND: expand-on-demand with an in-memory cache, newest review open on load, preview line
  under each closed row, and accordion state keyed by ID rather than index (it used to point at
  the wrong review after a search). Restored numbering on ordered lists, which the diamond bullet
  had erased, and dropped the dead renderMarkdown helper.
  SHIPPED IN THE SAME COMMIT (not in its message — note it here): `_load_json_disk_cache()` /
  `_save_json_disk_cache()` + disk persistence for ALL THREE scanner caches —
  volume_leaders_cache.json / reversion_cache.json / gainers_cache.json, loaded at module import
  and saved on every successful payload. Same pattern as the FRED disk cache: a service restart no
  longer serves an empty cache, and the stale-cache fallback paths now survive reboots. Both
  helpers are try/except-silent by design.

JUL 29 2026 (Wed) — CUSTOM LOGIN GATE + REVERSION FUNNEL DIAGNOSTICS  ** NOT COMMITTED **
=========================================================================================
>> SUPERSEDED — see the JUL 29-30 (night) entry below. Everything in this entry was committed and
   pushed on the night of Jul 29-30 (login gate = 8c64d41, funnel = 26a9f50). The git-state
   warnings and the "NOT YET OBSERVED" funnel note are HISTORICAL; kept for the reasoning trail.

!! READ THE GIT STATE BLOCK AT THE BOTTOM FIRST. Today's work is LIVE ON PROD but NOT in git.
   Server api.py md5 == the LOCAL WORKING TREE (5f38ac09...), not HEAD (535f0824...). This is the
   exact deployed-not-committed pattern that caused the Jun-7 refactor regression (6 features
   silently wiped). COMMIT BEFORE ANY FURTHER EDIT, or the next refactor built off HEAD deletes it.

CUSTOM LOGIN GATE (replaces the browser's native Basic Auth dialog) — DEPLOYED, frontend LIVE
  (server dist/index.html -> index-Wo6oMRja.js, the local build, contains the sp_auth key):
  frontend/src/lib/auth.js (new, untracked) — patches window.fetch ONCE at import so every
    existing fetch('/api/...') call in App.jsx picks up the stored Authorization header with no
    call-site changes. Credentials live in sessionStorage ONLY (cleared when the tab closes),
    never localStorage, never in a URL or a log line. A 401 on any /api/ call while credentials
    are stored fires notifyAuthLost() -> clears + kicks back to the login screen.
    verifyCredentials() deliberately bypasses the patched fetch so a wrong password at the login
    screen cannot trip the auth-lost path.
  frontend/src/LoginGate.jsx (new, untracked, 193 lines) — wraps <App/> in main.jsx; revalidates
    stored credentials against /api/health on mount.
  api.py — HTTPBasic(auto_error=False) + credentials typed Optional, and the
    "WWW-Authenticate: Basic" header REMOVED from the 401. BOTH changes are load-bearing and
    intentional: FastAPI's default HTTPBasic raises its own 401 with that header before
    _require_auth ever runs, and that header is precisely what makes the browser pop its native
    dialog — including on fetch/XHR calls from our own login screen. Missing credentials now
    arrive as None and are handled identically to a wrong password.
    Auth still FAILS CLOSED: the ENABLE_AUTH-with-empty-BASIC_AUTH_USERS 503 path is untouched.
    Machine clients are unaffected because they send explicit headers already (live_tracker.py,
    warm_volume_cache.sh) — but any FUTURE client that relied on the challenge header will not
    be prompted. Keep that in mind before "restoring" the header.

REVERSION FUNNEL DIAGNOSTICS (the direct follow-on to Jul-26): the warm cron is now firing
  reversion-leaders and returning HTTP 200 — and setup_log.reversion_hunter is STILL 0 rows.
  So a per-sub-condition journal line was added to get_reversion_leaders: one
  `[reversion-funnel] {...}` JSON line per call counting quotes / screener_pass / ml_na / ml_buy /
  rsi_none / oversold / vwap_none / below_vwap / deep_buy / potential_bounce / fk_downgrade /
  logged, emitted on BOTH the empty-screener early return and the normal path. Verdict logic is
  NOT touched — this only makes "genuinely rare" vs "one sub-condition never fires" decidable.
  NOT YET OBSERVED: deployed at 23:00 IL with the market closed, so zero funnel lines exist in
  the journal so far. First real data is tomorrow's in-window warm runs (from 16:00 IL / 09:00 ET).
  READ THEM FIRST before touching anything reversion-related.
  .gitignore (uncommitted): adds the three new scanner cache json files from a070db2.

LIVE STATE VERIFIED Jul 29 2026 (read-only checks):
  service active; mem peak 209MB (e2-medium still hugely over-provisioned); disk 27%.
  Watchdog 09:30 digest ALL GREEN including the new check #6 (tracker + intraday + wedge fresh).
  Resolver 03:00: candidates=50 attempted=50 resolved=44 stale=0 failed=0 (the 6 misses are
    yfinance "possibly delisted" noise, e.g. SEM — not a resolver defect).
  setup_log: gainers 705 rows (151 of them since Jul-27 — the warm fix is clearly working),
    volume_leaders 367 (41 since Jul-27), reversion_hunter STILL 0 EVER, resolved total 840,
    lev_sent populated on 565 rows.
  tracker.db: 140 signals, resolved-with-beta 92 (was 78 at the sitting — Track 1's re-run
    trigger is more TRADING DAYS, not row count).
  Local test suite: 61 tests green ON THE UNCOMMITTED TREE.
  warm log: exactly ONE HTTP 000 in its whole history, at 20:00Z today = the same minute as the
    api.py deploy restart. Almost certainly the restart, but confirm it does not recur.

STILL OPEN:
  !! COMMIT + PUSH. a070db2 is committed but NOT pushed (origin/main = 709bc52), and the entire
     Jul-29 login-gate + funnel change set is deployed-not-committed. Two separate gaps.
  Read tomorrow's [reversion-funnel] lines -> decide whether reversion_hunter is rare or broken.
  CLAUDE.md line counts are stale: api.py is 1778 lines (doc says 1387), scanners.py 624 (520),
     db.py 465 (316). CLAUDE.md also does not yet document the login gate, the WWW-Authenticate
     removal, the scanner disk caches, or the /api/recommendations/{id} split.
  Lev display labels around semis 0.37 — unlocked at the sitting, display-only, not done.
  Resolver does not capture forward-SPY -> Track 1's market-adjusted basis cannot run live
     (retroactive yfinance computation is legitimate: outcomes are not forward-only data).
  H1/H2 re-run once BREAKOUT CONFIRMED resolves past n>=15 per bucket under the NEW post-Jul-26
     sample composition. Do not loosen bucket definitions.
  EXPLORATORY-ONLY, do not act: BREAKOUT CONFIRMED shows negative median net ret_5d across all
     21 resolved rows. Needs its own pre-registered question first.
  frontend/dist/assets on the server holds 83 old JS bundles / 58MB — deploy.sh copies the full
     dist and never prunes. Harmless at 27% disk, but it is unbounded growth.
  VM Manager + Network Intelligence Center — disable in Console if still not done.
  Service still runs as User=root (Phase 2); extend NYSE_HOLIDAYS for 2027; v_accel UX.

NEXT MILESTONES:
  Jul 30    — first [reversion-funnel] output from the in-window warm runs.
  ~Aug      — ICE (Jul-23 BUY @ 0.76) resolves; tracker trading-day count grows toward a Track 1
              re-run (days, not rows).
  Months    — lev Stage B outcome test N=50 (BREAKOUT CONFIRMED-restricted); candidate for
              re-registration on a broader signal set rather than an indefinite wait.
  Nov 2026  — restore MIN_PRECISION_BUY 0.30 -> 0.48 (regime-mix criterion, not calendar).
  Nov 27 / Dec 24 — first half-day sessions exercising session_close_hour().

GIT STATE (Jul 29 2026) — THE MESSIEST STATE THIS PROJECT HAS BEEN IN SINCE JUN 7. Three layers:
  origin/main = 709bc52 (Jul 26)
  local main  = a070db2 (Jul 28, daily reviews tab) -- COMMITTED, NOT PUSHED
  prod server = local WORKING TREE (api.py md5 5f38ac09) -- DEPLOYED, NOT COMMITTED:
                  M .gitignore        (3 scanner cache json files)
                  M api.py           (auth auto_error/Optional/no-WWW-Authenticate + funnel log)
                  M frontend/src/main.jsx (LoginGate wrapper + auth.js import)
                  ?? frontend/src/LoginGate.jsx
                  ?? frontend/src/lib/auth.js
  scanners.py and db.py are unchanged and in sync across all three. 61 tests green on the working
  tree. Commit and push all of the above before starting anything else.

JUL 29-30 2026 (Wed night) — DATA-COLLECTION IMPROVEMENT SEQUENCE: 3 SHIPS + GIT GAP CLOSED
===========================================================================================
Session question was "how do we improve data collection?" — answered in three ships, deliberately
ordered CHEAPEST-DIAGNOSTIC-FIRST so instrumentation started accumulating while the bigger code
was written. All four commits pushed; local == server == origin/main (md5-verified on api.py,
scanners.py, db.py, live_tracker.py).

  8c64d41  login gate + auth.js + LoginGate.jsx + .gitignore  (the Jul-29 deployed-not-committed
           gap CLOSED — committed separately from today's work so the auth change keeps its own
           reasoning trail; see the superseded Jul-29 entry above for the full design notes)
  26a9f50  [reversion-funnel] diagnostics
  e373ae5  screener plausibility guard (3 endpoints, 6 new unit tests -> 67 green)
  e3cceff  forward-SPY capture in BOTH resolvers

STEP 1 — REVERSION FUNNEL (26a9f50) — AND THE ANSWER IT ALREADY GAVE
  One [reversion-funnel] JSON line per non-cached run, per sub-condition. Reviewer asked for the
  Oversold leg to be split to sub-conditions in THIS round rather than a second deploy — done, so
  the first line was already conclusive. Log-only: no DB writes, no verdict changes, no schema.
  FIRST LIVE READING (Jul 29 23:01 IL, repeated identically at 23:17):
    quotes=50 screener_pass=25 below_vwap=25 oversold=17 rsi_none=0 vwap_none=0
    ml_buy=0 ml_na=17 deep_buy=0 potential_bounce=0 fk_downgrade=0 logged=0
  READ IT AS: the technical legs are HEALTHY (every candidate below VWAP, 17 oversold, no missing
  RSI/VWAP data). The pipeline is NOT broken. TWO stacked causes of the 0-row history:
    (a) ml_buy=0 — the known 0.70 confidence drought, and
    (b) ml_na=17/25 — most day-losers are NOT in the sp500/nasdaq100 scan caches and do not get an
        ML opinion at all inside ThreadPoolExecutor(max_workers=5)/timeout=30 on a 2-shared-vCPU
        box. The very population this tab exists to catch (non-index mid-caps) is the population
        that falls outside the prediction budget.
  => "reversion_hunter = 0 rows in 48 days" is EXPLAINED. Not a call-site bug (already verified
     wired at api.py) and not the Jun-7 dropped-call-site pattern.
  OPS BONUS FOUND: the journal is readable WITHOUT sudo — elimaoz99 is in the `adm` group. Use
     journalctl -u stock-app.service | grep reversion-funnel     (no sudo, no password prompt)

STEP 2 — SCREENER PLAUSIBILITY GUARD (e373ae5) — closes the Jul-13 TODO
  Reviewer pushed back on the original FRED-analogy sketch on two points; BOTH were adopted, and
  they are the reason the design is better than the queued TODO:
   1. TRIGGER IS AN INTRA-SESSION FLAP, NOT AN ABSOLUTE THRESHOLD. Compare the RAW quote count to
      the max seen earlier in the SAME trading session (market_calendar.us_trading_date key);
      suspect when new < 50% of that baseline (_GUARD_RATIO). Rationale that killed the obvious
      false-positive ("what if the market really only had 6?"): a genuinely thin market opens thin
      and STAYS thin — only a partial payload flaps mid-session, exactly as on Jul 13 (25 rows then
      6 rows, 24 seconds apart).
   2. ON TRIP: SERVE THE CACHE DISPLAY-ONLY, WRITE NOTHING. Early return before any processing, so
      zero setup_log rows are ever born from stale features. This REPLACED the reviewer's proposed
      is_fallback=True column: a flag pushes the burden onto every future backtest/analysis author,
      while skipping the write keeps the dataset clean by construction. USER CHOSE THIS EXPLICITLY.
  Pure leg scanners.screener_payload_suspect() is unit-tested (incident replay 25->6, strict
  boundary at exactly ratio*prev, no-prior-payload, growing payload, custom ratio). State wrapper
  api.py _screener_guard() covers volume_leaders + reversion_hunter + gainers.
  No same-session cache -> the thin payload passes through (thin truth beats nothing).
  3 CONSECUTIVE trips -> Telegram alert (Yahoo API change / rate limit), plus a [screener-guard]
  journal line on every trip. Baseline resets per session and per restart, deliberately NOT
  persisted to disk (a restart mid-glitch re-baselining thin is accepted best-effort).

STEP 3 — FORWARD-SPY AT RESOLUTION (e3cceff) — closes the sitting's Track-1 basis gap
  The Jul-24 sitting found tracker outcomes hold only RAW fwd_ret and entry_spy_ret5d is
  trailing-at-entry, so the dual-basis net-of-SPY leg had NEVER RUN. Now captured live:
    live_tracker.py  outcomes.spy_fwd_ret (idempotent ALTER; FRACTION, same units as fwd_ret) —
      SPY closes memoized per run over the exact entry/exit dates.
    db.py setup_resolve  setup_log.spy_ret_1d / spy_ret_5d (PERCENT, same units as ret_1d/ret_5d) —
      ONE batch SPY download spanning the whole night's rows, then per-row returns indexed by the
      STOCK'S OWN BAR DATES so the SPY window matches ret_1d/ret_5d exactly (holidays/halts align
      naturally, no NaN, no busday arithmetic).
  MISSING-DATA POLICY (deliberate, differs from the reviewer's "write NULL and retry"): an
    outcomes/resolved row is written EXACTLY ONCE, so resolving without SPY would leave a PERMANENT
    null from a merely transient fetch failure. Therefore: tracker skips that row this run (retried
    next run); setup_resolve defers the WHOLE batch one night if the SPY download fails.
  ALSO REJECTED (reviewer's live_forward_spy/closed_forward_spy pair): there is no open-window
    problem to manage. Both resolvers only write once the holding window has CLOSED (setup_log rows
    stay resolved=0 until closes[5] exists, up to the 30-day tombstone), so forward-SPY is static
    at birth. A daily-updating column would have introduced the first MUTABLE field into an
    append-only research log — a step backwards.
  THESE ARE OUTCOME COLUMNS WRITTEN AT RESOLUTION. The forward-only rule governs SIGNAL-TIME
    features and is untouched. Rows resolved BEFORE Jul 30 keep NULL spy — legitimately
    retro-computable in analysis if ever wanted, because outcomes are not forward-only data.
  VERIFIED LIVE, three ways:
    tracker: resolved NVDA + ABBV with spy_fwd_ret = -2.83% (NVDA -8.38% => alpha -5.6%;
             ABBV +3.50% => alpha +6.3%) — first dual-basis rows the project has ever had.
    setup_log: synthetic AAPL row dated Jul-13 resolved to ret_5d +2.92 vs spy_ret_5d -0.95 (it
             spans the Jul-16 selloff, so the sign split is the expected shape), THEN DELETED —
             verified 0 synthetic_test rows remain. Same synthetic-write technique that exposed the
             root-owned falling_knife_log.db on Jul 10.
    defer path: a 5-row and a 20-row batch both ran clean; the SEM "possibly delisted" yfinance
             noise is pre-existing and unrelated.

DOCS SWEEP (CLAUDE.md, Jul 30) — the handoff had flagged all of these as stale/missing:
  line counts refreshed (api.py 1820 / scanners.py 638 / db.py 504 — doc said 1387/520/316);
  login gate + the load-bearing WWW-Authenticate REMOVAL documented with a do-not-restore warning;
  3 scanner disk caches added to the Databases table; /api/recommendations/{id} split added to Key
  endpoints; funnel + guard + forward-SPY each documented in their own section; "THE SITTING"
  pending-action block rewritten to CLOSED + code-freeze LIFTED (it still read as if upcoming);
  H1/H2 re-run, the exploratory BREAKOUT CONFIRMED red flag, lev display labels, and the 58MB
  dist-bundle growth moved into Pending actions.

STILL OPEN (carried forward)
  PREDICTION-BUDGET QUESTION — the one real decision this session opened, deliberately NOT taken.
    Collect ~1 week of [reversion-funnel] lines first, then choose: (a) raise the 30s timeout —
    simple but slows cold-cache responses and loads a 2-shared-vCPU box we downgraded on purpose;
    (b) persistent per-symbol prediction cache with TTL, so each 25-min warm cycle judges a few
    more names and coverage ACCUMULATES across the day instead of being crammed into 30s — likely
    the strongest option; (c) accept "indexed losers only" as the tab's effective universe.
    WHY WAIT: if ml_buy stays 0 anyway (the 0.70 drought), more coverage buys nothing — and
    widening coverage CHANGES SAMPLE COMPOSITION, which is a new measurement regime that must be
    pre-registered, exactly like the Jul-26 warm fix. Not an incidental tweak.
  Lev display labels around semis 0.37 (display-only frontend, unlocked at the sitting).
  H1/H2 re-run at n>=15 per bucket under the post-Jul-26 composition; do not loosen buckets.
  BREAKOUT CONFIRMED negative-median-net observation: EXPLORATORY ONLY, needs its own
    pre-registered question before any action.
  Server dist/assets ~83 bundles / 58MB, unbounded (deploy.sh never prunes). Harmless at 27% disk.
  VM Manager + Network Intelligence Center still billable in Console; service still User=root
    (Phase 2); extend NYSE_HOLIDAYS for 2027; v_accel UX.

NEXT MILESTONES
  Jul 30 03:00 IL — first resolver run writing spy_ret_1d/spy_ret_5d on REAL rows (the synthetic
                    proof already passed). 20:05 tracker run does the same for spy_fwd_ret.
  ~1 week        — enough funnel lines to settle the prediction-budget question.
  ~Aug           — ICE (Jul-23 BUY @ 0.76) resolves; tracker trading-day count grows toward a
                   Track-1 re-run (trigger is DAYS, not rows: 23 days / 92 rows as of Jul 29).
  Months         — lev Stage B outcome test N=50, BREAKOUT CONFIRMED-restricted (N=4 as of Jul 29);
                   candidate for re-registration on a broader signal set rather than waiting.
  Nov 2026       — restore MIN_PRECISION_BUY 0.30 -> 0.48 (regime-mix criterion, not calendar).
  Nov 27 / Dec 24 — first half-day sessions exercising session_close_hour().

GIT STATE (Jul 30 2026) — CLEAN, all three layers identical again:
  origin/main = local main = prod server = e3cceff
  md5-verified across api.py / scanners.py / db.py / live_tracker.py.
  67 tests green. Only this handoff file is intentionally left uncommitted as a working journal.

JUL 31 2026 (Fri) — REVIEW ROUND 2: p(BUY) DIAGNOSTICS, GUARD LEG 2, STAGE B SAMPLE RESET
==========================================================================================
  1b2868b  all three items (74 tests green; core_logic.py scp'd separately — deploy.sh
           only ships api/scanners/db, so ANY core_logic change needs its own scp+restart)

ITEM 1 — STAGE B SAMPLE RESET (zero-code decision, done FIRST and BLIND)
  Found while pricing the reviewer's regime-shift concern: the Jul-26 warm-cron fix is a
  SAMPLE COMPOSITION BREAK. BREAKOUT CONFIRMED accrual by week: W28=1, W29=1, W30=14.
  Pooling pre- and post-fix rows into one N=50 Stage B sample would mix sampling regimes
  inside the confirmatory test itself.
  DECIDED: floor Stage B eligibility at 2026-07-27; N restarts at 0 (verified live:
  "NotEnoughData: N = 0 ... No outcome columns were read"). Chosen over carrying the 4
  pre-fix rows with an is_post_fix control factor — cleaner at small N, and the new rate
  reaches 50 in weeks.
  UNCHANGED: boundary semis 0.37 and min_n 50. The boundary is a MARKET-level covariate
  distribution (identical for every row of a given scan), so it is independent of which
  stocks get logged — the reset touches the outcome-test pool only.
  MECHANISM (reusable): amendments are ADDITIVE WRITE-ONCE FILES, never edits to
  lev_spec_frozen.json — its refuse-overwrite property is what makes the boundary
  trustworthy. lev_spec_amendment_1.json carries its own timestamp/reason/refuse-overwrite;
  stage_b() loads it and prints it loudly before the count gate.
    lev_sitting.py --amend-sample-start YYYY-MM-DD --amend-reason "..."   (reads no DB column)
  BUG CAUGHT WHILE IMPLEMENTING: stage_b()'s data SELECT still used the UNFLOORED
  ELIGIBLE_WHERE while the count gate used the floored one — the analysis would have
  silently pulled the pre-reset rows the gate excluded. A pre-registration guard applied in
  only one of two places is worse than none. Both now use the same `where`.
  RATE CAVEAT: W30's 14/wk projects to N=50 in late August + ~7d resolution lag, but W27 hit
  9 with no fix in place, so the burst is partly market-driven. The trigger is the COUNT,
  never the calendar; a checkpoint may evaluate N and accrual rate ONLY, never outcomes.

ITEM 2 — p(BUY) IN THE FUNNEL (the reviewer's best question, with a corrected instrument)
  Question: is ml_buy=0 a THRESHOLD problem or does the model reject beaten-down names
  outright? The proposed instrument (avg/max `confidence`) would have answered WRONGLY:
  confidence = proba[pred_idx] = probability of the WINNING class, so HOLD@0.85 says nothing
  about BUY-ness, and a 3-class max is >= 1/3 BY CONSTRUCTION (the "max score 0.22" scenario
  is structurally impossible).
  SHIPPED: core_logic._p_buy(proba, classes) -> raw BUY-class probability, captured
  PRE-options-adjustment so it stays a pure model output. Wired through both prediction
  paths (get_prediction + scan_symbol).
  PERSISTED: scan_results.proba_buy (idempotent ALTER + save/read). Necessary, not optional:
  in-session most day-losers come from the sp500/nasdaq100 scan cache, so without this the
  funnel would only see p(BUY) for the uncached minority — exactly the biased subset.
  Old cached rows carry NULL until the next scan; get_latest_scan uses a "proba_buy" in
  r.keys() guard.
  FUNNEL now also logs p_buy_n / max_p_buy / avg_p_buy.
  FIRST LIVE READING (13:10 IDT, pre-market): p_buy_n=17 max_p_buy=0.661 avg_p_buy=0.44
  => CEILING, not categorical rejection. The model gets within 0.04 of the 0.70 gate on the
  best loser and averages 0.44 across the field. Do NOT act on one pre-market line.
  DIAGNOSTICS ONLY — no gate, no verdict change, nothing written to setup_log.

ITEM 3 — GUARD LEG 2: HOLLOW PAYLOADS (scanners.screener_quotes_malformed, 7 new tests)
  Real gap the reviewer found: the guard compared len(quotes) only, so a payload that keeps
  its usual 50 objects but arrives with price/volume fields None/0 sails through — the
  filtered list collapses and the rich cache is overwritten anyway, i.e. the Jul-13 damage
  in a different disguise.
  Checked on RAW quotes (majority missing regularMarketPrice/regularMarketVolume), which
  keeps it MARKET-INDEPENDENT. Note why monitoring the FILTERED count instead would be
  wrong: filtered size legitimately swings with the market (7 day_losers at Thursday's open
  vs 25 by the close) and would false-positive constantly.
  Never re-baselines on a hollow payload, so one bad response cannot lower the bar for the
  next; 3 consecutive -> Telegram. _screener_guard now takes `quotes` instead of len(quotes).
  (The reviewer's field names 'volume'/'close' are not the payload's — the Yahoo screener
  keys are regularMarketVolume / regularMarketPrice.)

ITEM 4 — ml_na ALERT: DROPPED, agreed. journald is PERSISTENT (/var/log/journal, 1.5GB,
  entries back to Apr 17) so a week of funnel lines is safe with zero new code; grep at the
  end of the week answers it.

CORRECTION LOGGED (my own, from Jul 30): the "cold start" explanation for ml_na=17 was
  WRONG. The Jul-30 lines split by clock (off-hours ml_na=17 on an IDENTICAL frozen 25-name
  list; in-session 0-4), not by process age — two of the 17-lines were hours after any
  restart. AND a Jul-31 pre-market line showed ml_na=3, which breaks the simple clock story
  too. Treat the driver as unsettled: likely which symbols land on the list plus whether the
  live predictions complete. The week of lines will settle it.

STILL OPEN: unchanged from Jul 30 (prediction budget pending ~1wk of funnel lines, lev
  display labels, H1/H2 at n>=15/bucket, BREAKOUT CONFIRMED negative-median EXPLORATORY
  ONLY, dist/assets 58MB, VM Manager + Network Intelligence Center, User=root, NYSE_HOLIDAYS
  2027, v_accel UX).

GIT STATE (Jul 31 2026): origin/main = local = prod = 1b2868b (+ docs commit on CLAUDE.md).
  74 tests green. Only this handoff file is intentionally uncommitted.


AUG 2 2026 (Sun) — TRACKER REPORT BUG, MEMORY INDEX REWRITE, COHORT BACKED UP OFF-VM
=====================================================================================
  a5ccc07  live_tracker --report display fix
  007f471  CLAUDE.md git-state refresh (superseded next day — see Aug 3)
  ffdeead  lev_spec_frozen.json into the repo + lev_spec_provenance_1.json

Market closed (Sunday). Session started as "where are we with the research" and turned into
  two bug finds and one real single-point-of-failure closure.

ITEM 1 — RESOLVER IS HEALTHY; THE 5-SESSION OFFSET LOOKS LIKE A STALL AND IS NOT
  setup_log rows dated Jul 27-31 all showed resolved=0 with ret_1d NULL. That reads like the
  Jul-9 starvation bug returning. It is not. A row is written ONCE, only when ret_5d exists,
  i.e. 5 sessions later: Jul 24's 11 rows resolved in the Aug 1 run, and the Aug 2 run
  correctly had candidates=0. Jul 27 tranche lands in the Aug 4 run.
  RULE OF THUMB: before calling the resolver stalled, check the 5-session offset first.
  Stage B pool: 16 BREAKOUT CONFIRMED banked Jul 27-31, lev_sent filled 16/16, N still 0
  because none has matured. ~16/week -> N=50 mid-to-late August plus the lag.

ITEM 2 — live_tracker --report HAS BEEN PRINTING RETURNS 100x TOO SMALL (fixed, a5ccc07)
  fwd_ret is stored as a FRACTION. resolve_outcomes() multiplies by 100 before appending, so
  the Telegram per-row lines were right — but print_report reads raw DB rows and printed them
  with a % suffix and no scaling. Every Ret in the table AND the "Avg ret" summary (which the
  Telegram summary line consumes from the same stats dict) were understated 100x.
  Displayed -0.03% was really -2.59%. Header "Avg ret: -0.00%" was really -0.35%.
  tracker.db itself was never affected — data correct throughout, only the reader was wrong.
  SECOND BUG, same commit: the header claimed "Long-only Expert (>=70%)" but log_signals()
  has NO confidence filter at all — it logs every signal=='BUY' row, i.e. the full BUY tier
  from >=0.57 scanner-side. CONF_MIN is only the /api/scan request param. Header now states
  the real sample plus the hit definition (ret >= 3% in 10 trading days), which the old label
  never disclosed.
  CONSEQUENCE FOR ANY PAST READING: pre-Aug-2 "avg ret" quoted from the report or Telegram was
  100x too small. PRECISION figures are unaffected — hits were always counted correctly.
  Current: 116 resolved, precision 33.6%, avg ret -0.35%, 35 pending. Question B (walk-forward
  retraining) needs N>=200 with consistent model_version; 112 of the 116 qualify. ~15-20
  signals/week -> N=200 around mid-September including the 10-day resolution lag.
  EXPLORATORY, NOT A RESULT: bucketing the 116 by confidence gives a monotonic gradient
  (>=70%: 24 rows, 45.8% hit, +2.4% avg | 63-70%: 48 rows, 39.6%, -0.34% | <63%: 44 rows,
  20.5%, -1.85%). Bucket edges were chosen by me AFTER seeing the data — textbook HARKing.
  Do NOT raise any threshold on this. It is a hypothesis to pre-register before N=200.

ITEM 3 — MEMORY INDEX REWRITTEN (agent auto-memory, not repo)
  MEMORY.md had grown into paragraphs duplicating the memory files themselves, and was showing
  Jul-31 state while the files already held Aug-2 state. Cut 8,622 -> 4,308 chars: one pointer
  line per memory, content stays in the files, guardrails kept visible in the pointers.

ITEM 4 — THE COHORT HAD ZERO COPIES OUTSIDE THE VM (closed)
  Discussion of "where should research live" surfaced the actual risk, which was not file
  format. Findings, in order of how much they changed the picture:
  (a) lev_spec_amendment_1.json does NOT cite a hash of the frozen spec. The provenance
      question the review asked - "is there a hash + time trace for 0.37 from before the
      outcomes" - answers NO on hashes.
  (b) BUT lev_spec_frozen.json stores its full derivation (13 day-medians, n_days, n_rows),
      and it REPRODUCES: recomputing from setup_log.db gives 13/13 day-medians matching to the
      last digit, n_rows 362, boundary 0.37 / 0.47 exactly. For a deterministic function of
      recorded data the strong evidence is REPRODUCIBILITY, not provenance — you need not
      trust when 0.37 was written if anyone can recompute it.
  (c) What reproduction does NOT establish: that the rule FAMILY was chosen blind. That rests
      on lev_sitting.py being committed 65e32a1 on Jul 14, ten days before frozen_at, plus
      Stage B never having run. Both under our control. Call it discipline with a code trail,
      not third-party certification.
  (d) THEREFORE the asset is setup_log.db, not the JSON. The JSON regenerates; the 362 rows of
      signal-time covariates cannot (forward-only forbids backfilling them). And it had NO
      copy off the VM. That was the real single point of failure the whole time.
  TWO CORRECTIONS I OWE MYSELF: "a commit timestamp cannot be forged" is FALSE (GIT_AUTHOR_DATE
  is an env var). "If the VM dies the proof dies" is FALSE for the number — it regenerates.
  BACKUP DONE (read from server, write to Mac, live DB never stopped or modified):
    VACUUM INTO snapshot, sqlite 3.45.1 via venv/bin/python3 (no sqlite3 CLI on the VM)
    sha256 85714536c652ab2e768a41845bf60d26d875dded85f4379205c20c6fc7e11cc7
    1185 rows, cohort 362 rows over 13 days; hashed on the VM and re-hashed after transfer
    live file re-hashed after the VACUUM — unchanged, so no write landed mid-operation
    RESTORE VERIFIED, not just bytes: recomputed 0.3700 / 0.4700 FROM the Mac copy
    dropped in ~/Desktop/Stock-predictor/_db_snapshots/ with SHA256SUMS.txt; *.db gitignored
  Also pulled the two setup_log.db.bak files (Jun 21, Jul 7) off the VM. IMPORTANT NOT TO
  MISREMEMBER: they are pre-cohort fossils, not a snapshot chain — both predate the lev columns
  (deployed the evening of Jul 7), so they can falsify a rewrite of rows BEFORE Jul 7 and say
  nothing about the 362-row cohort. For the cohort itself there was no snapshot before this one.
  DECOY REMOVED: the Mac had setup_log.db with 0 rows (schema stub from a Jul-10 local run) —
  right name, right place, would quiet any search. Renamed to
  setup_log.EMPTY-SCHEMA-DO-NOT-USE.2026-07-10.db, not deleted.
  WAL NOTE: sha256 of a WAL database is only the full logical content when no -wal sidecar
  exists at that instant. Use VACUUM INTO for a consistent snapshot without stopping anything.
  STILL OPEN: no third copy outside the two machines. GCS with versioning is the right target
  (survives VM deletion); it is a cost + setup decision, not a copy. iCloud rejected as a quiet
  default — nearly unused here, and it would ship trading research to a provider not chosen.

ITEM 5 — REPO CONFIRMED PUBLIC (api.github.com, unauthenticated 200)
  Flagged only AFTER pushing lev_spec_frozen.json + the provenance record. Should have been
  connected before the push, not after. Data is not exposed (*.db gitignored, _db_snapshots/
  added to .gitignore) but methodology and thresholds are. Decision deferred deliberately.


AUG 3 2026 (Mon) — CLAUDE.md TRIM: GUARDRAIL INVENTORY, STALE GIT-STATE OUT, PENDING MOVED
===========================================================================================
  09539d3  stage 0 — _guardrail_inventory.md + check_guardrails.py (no CLAUDE.md change)
  b8923f1  stage 1 — delete both git-state sections, add the no-live-state rule
  d260cb0  stage 2a — lift 5 research rules out of Pending actions
  0f43ad8  stage 2b — Pending actions -> PENDING.md, pointer left behind

CONTEXT: an Anthropic piece on context engineering for Claude 5 (Claude Code deleted 80% of its
  own system prompt with no eval loss). Measured our file: 497 lines, 45,924 chars, ~11.5k
  tokens loaded EVERY session, over the ~40k large-memory warning threshold.
  The /doctor run of Aug 2 could not see any of this — it ran from ~ and the project CLAUDE.md
  is in ~/Desktop/Stock-predictor. Right tool, empty room. Run it FROM THE PROJECT DIRECTORY.

THE SELF-INFLICTED BUG (the reason stage 1 is a bug fix, not a cleanup)
  Aug 2 I added a git-state block to CLAUDE.md — content the a5ccc07 commit message already
  carried verbatim. By that evening HEAD was ffdeead and the block said a5ccc07, while an older
  Jun-7 section said 9fe3557. Two sections, both wrong, contradicting each other, in
  always-loaded context. That is not wasted tokens, it is context poison: the model reconciles
  two stale values instead of reading git log.
  SAME FAILURE CLASS as the N=16/50 problem. The rule is about the KIND of information, not
  the filename. Anything that changes without an edit here does not belong in a constant context.

STAGE 0 — THE SAFETY NET (this is what makes the rest safe)
  _guardrail_inventory.md: 77 IRON guardrails quoted verbatim, stable GR-nnn ids, source
  section, extracted by a FULL READ of all 497 lines. Not by grep: grepping never/do-not/must
  returned 50 hits, and several were file descriptions that merely contained the word
  (get_lev_sentiment, get_sector_heatmap, watchdog.py). grep counts words, not rules. The full
  read found 77 actual rules, including ones with no imperative in them at all (SELL->HOLD,
  period="6mo" NOT 3mo, Wilder's smoothing).
  check_guardrails.py: every IRON quote must appear verbatim in CLAUDE.md (or in a skill once
  stage 4 moves WORKFLOW content). Non-zero exit = roll back the step. Refuses to pass
  vacuously on an empty inventory. Green on baseline 77/77, and after every step since.
  THE CATCH THAT JUSTIFIED THE WHOLE EXERCISE: Pending actions was NOT pure state. GR-019..023
  are IRON rules physically inside it (write-once amendments, closed-confirmatory family, do
  not loosen buckets, exploratory-only, budget change = sample composition change). Moving the
  section in one step would have carried five guardrails out of always-loaded context.
  Hence stage 2 split into 2a (extract rules) then 2b (move state), never one commit.
  REJECTED — HTML id anchors instead of verbatim quotes: 77 anchors cost ~288 tokens
  permanently in the file we are shrinking, and an id-only check passes even if the rule text
  under the marker is deleted. That fails OPEN, which is the Jun-7 shape. Kept exact match and
  shortened the 5 longest keys (141 -> ~70 chars) to their operative core instead. A checker
  failure after an intentional reword means "update the inventory in the same commit" — the
  friction is the mechanism, a net that updates itself is not a net.

PENDING.md AND ITS RULE (why not "30 days -> archive")
  Every item declares TRIGGER (a condition plus THE COMMAND that answers it — never expires) or
  REVIEW (a date). An item with neither is the trash, and that is what gets swept.
  A calendar sweep would have deleted the Stage B trigger, the N>=50 instrumented-gate
  calibration and the per-regime analysis — count-triggered pre-registrations that are SUPPOSED
  to sit for months. Same category error as the drought criterion deferred in July as
  mis-specified. Triggers name commands rather than restating counts, so this file cannot rot
  the way the git-state blocks did. Closing = move to Archive with an outcome line, append-only.
  All 15 old items verified present in the new file by probe, not by eye.

RESULT SO FAR: 45,238 -> 41,420 chars, ~950 tokens/session. Only 8% — deliberately. What came
  out is what was HARMFUL (contradictory git state, a research queue pulling every session
  toward itself), not what is BIG. The bulk (architecture line counts, ENV table, feature specs,
  the downgrade and restoration sagas) is derivable or narrative and belongs to stage 3.
  STAGES 3-4 NOT STARTED: classify every section KEEP_GOTCHA / SKILL / DERIVED_DELETE /
  NARRATIVE_DELETE / STATE_DELETE as a report first, execute only after approval. Skills to
  extract: post-deploy checklist, DB debug queries. IRON never moves to a skill — a skill loads
  only when called, and these must be present even for an innocent question.
  Stopping mid-way is a legitimate outcome; 8k with full IRON is a win.

HONEST GAP IN THE INVENTORY: the deleted a5ccc07 paragraph contained one real gotcha, not just
  narrative — that tracker.db pools the full BUY tier (>=0.57), not >=0.70. I classified the
  whole block as narrative, so the checker would not have caught its loss. It survives because
  it is now in live_tracker.py's own header and an inline comment, which is a stronger location
  than prose. But the inventory is not perfect and should not be trusted as if it were.

STILL OPEN (moved to PENDING.md — this handoff no longer duplicates the list):
  biggest remaining risk is unchanged and is not in this file's subject matter — no third copy
  of setup_log.db outside the VM and this Mac.


AUG 9 2026 (Sun) — ROUTINE HEALTH CHECK FOUND A 3-NIGHT SILENT BACKUP OUTAGE
===========================================================================================
  928162c  the backup died three nights on a wake race — retry, never fake a dated dir

HOW IT WAS FOUND: not by an alert. A "let's check the system" request on a Sunday morning.
  The server side was entirely green — service up 34 days, disk 28%, collection 100% lev_fill
  every session, resolver 50/50 nightly, all 12 crontab lines present, watchdog Telegram sent
  daily. The failure was on the ONE leg the server cannot see: the Mac-side pull.
  ~/stock-predictor-backups held 20260806, 20260807 and 20260808 as EMPTY directories.

THE MISDIAGNOSIS, AND WHY IT MATTERED. The log reads "Network is unreachable" (Aug 6) then
  "Connection reset" twice (Aug 7, 8), and the obvious story is "launchd fires on wake before
  Wi-Fi is up". I wrote that story down and would have shipped a preflight wait-for-network.
  Then /tmp on the VM turned out to hold two COMPLETE snapshots, dated Aug 7 08:15 and Aug 8
  08:12 — those runs cleared the first ssh, ran backup_dbs through three VACUUM INTOs, and died
  AFTER. A preflight probe would have passed on both nights and fixed nothing. What actually
  fixes it is retrying the whole pull(), which is what got built — by luck of ordering, not by
  the reasoning. LESSON: confirm a failure's STAGE from the far end's leftovers, not from the
  local error text. The two orphaned snapshots were also a third bug: the per-run temp cleanup
  sits in a finally that runs over the connection that just died.

THE EMPTY DIRECTORIES ARE THE REAL DEFECT. makedirs ran before the copy, so every failed run
  left a dated dir that reads to any future reader as a backup that happened. The whole pull
  design exists to prevent silent backup failure, and it had reintroduced the silent-failure
  shape inside itself. Files now stage in .incoming-* and promote() creates the dated dir only
  after SHA-256 verification. Rotation already ignored non-dated names, so staging is free.

DETECTION LAG WAS 4 DAYS AND THAT IS THE DESIGN WORKING, NOT FAILING. watchdog check 8 read
  age=59h on Aug 8 against a 72h threshold and stayed green; it would have gone red Aug 9 09:30,
  ~83h in. The 72h was chosen deliberately so a closed laptop over a weekend does not cry wolf
  while "stopped weeks ago" still trips. Do NOT lower it — the retry removes the need. Worth
  noting the failure would have self-announced eventually; it just cost four days of one-copy
  exposure first.

WHAT THE OUTAGE ACTUALLY COST: nothing lost, but Aug 6 and Aug 7 were sessions adding ~110
  forward-only setup_log rows that existed in ONE copy for four days. The Aug 8 (Sat) miss cost
  zero. Trading days do not gate the backup — it is a daily job — but they price its failure.

PROVEN ON THE PRODUCTION PATH, NOT JUST BY SELFTEST. The 08:00 automatic run went green the
  same morning and proves ONLY that nothing broke: an awake, online Mac succeeds on attempt 1
  and touches none of the new code (the log shows zero "attempt" lines). Real evidence came from
  pointing SERVER at 192.0.2.1 (TEST-NET-1, unroutable) against the INSTALLED agent copy —
  retry engaged 3x, surfaced as a plain RuntimeError not NetworkError, and left no dated dir and
  no staging dir. A scheduled green run is not a test of a failure path.

ALSO VERIFIED WHILE IN THERE (no action taken, recorded so the next session need not re-derive):
  R1 blind preview 22 rows / 10 days against gates 60 AND 25 — row gate binding, as designed.
  Stage B N=16 of 50. setup_log 1474 rows, Stage B pool 594 across 10 sessions since Jul 27.
  rvol_trend / hod_gap_ratio began filling Aug 6, partial by design (only in-gate-window rows).
  Aug 9 resolver returned resolved=49/50 with failed=0 — first non-50 in the visible window;
  watch, do not act.

SEPARATELY, A QUESTION ANSWERED WITHOUT A CODE CHANGE: scanner BUY count went 2-3/day mid-July
  to 10-13/day this week and the user asked whether we had moved a threshold. We had not — zero
  commits touched CONFIDENCE_THRESHOLD / SCAN_CONFIDENCE_THRESHOLD / PREMIUM_SCAN_THRESHOLD
  since mid-June, server == repo. The rise tracks regime: VIX 19-20 -> 15-16 and SPY 5d
  -1.5% -> +3.4..5.7%, with the inflection sitting exactly on the Jul 30 BULL_WEAK -> BULL_STRONG
  flip. The Jul drought self-resolved as predicted. conf>=0.70 signals went 0-1/day to 3/day on
  some days, which feeds R1 accrual directly. EXPLORATORY — cannot justify any code change, and
  more signals is not more precision (displayed precision 31-48%, unchanged). Note the tension
  it creates for R1: a regime-dependent accrual rate biases the cohort toward rising markets,
  which is precisely why the endpoint is alpha vs SPY on the same days and not absolute return.

STILL OPEN: unchanged from Aug 3 — nothing new opened today.

===========================================================================================
  (no commit)  Aug 13 — a week of analysis that shipped nothing, on purpose

WHAT HAPPENED: a Reddit thread on SPX options seeded a review sweep. Six rounds of
  propose-then-retract followed — two spec amendments, a covariate migration, a gate-
  calibration bundle, a funnel counter, a reversion-tab fix — every one withdrawn after
  its own audit. Zero deploys. The frozen specs (r1 5b369d9, lev) were never touched, and
  the one real artifact (a tested volume/atr14 migration in db.py) was deliberately
  reverted from the working tree after its rationale was preserved. LESSON, written as a
  rule: check WHO CONSUMES a covariate/counter/amendment BEFORE proposing it, measure
  offline before proposing a deploy, and propose at most once.

WHAT THE WEEK ACTUALLY PRODUCED (all exploratory, all preserved in PENDING.md → Parked):
  1. THE DEMOTION FINDING. Two post-threshold filters in core_logic.py (cmf<0 → HOLD,
     close<SMA50 → HOLD) demote ~65% of ALL model BUYs after they clear the confidence
     threshold — measured twice, independently agreeing: scan_results direct (241/353 of
     >=0.57 rows, 39 rows >=0.70) and a 6-month offline reconstruction over the frozen R1
     universe (baseline 64.8%, reversion <=-5% population 73.4%; the CMF leg is
     non-selective 56.7% vs 55.5% — the SMA50 leg carries ALL the selectivity).
     This single fact unified three "separate" mysteries: reversion_hunter's 0 rows ever
     (0.70 ceiling for the non-index majority x 73% demotion for survivors — NOT either
     alone), the p_buy diagnostic reading "grazing the ceiling" while ml_buy=0 (p_buy is
     captured BEFORE the filters — structurally blind to them), and beta-gate starvation
     (beta>1.5 AND verdict==BUY cannot fire when BUY is choked upstream).
     UNTOUCHABLE until the R1 unblind — the filters feed the frozen cohort, and demoted
     BUYs never reach tracker.db, so "were they actually worse?" is unanswerable without
     pre-registered demotion logging first.
  2. Prediction-budget question CLOSED (was open since Jul 30): ml_na 4-15 all week,
     fk_downgrade=0 on all 75 lines Aug 3-7. Change nothing.
  3. Backup verified USABLE, not just intact: independent SHA-256 vs the server heartbeat
     AND sqlite integrity_check ok AND row counts advancing (setup_log 1530→1591,
     tracker 205→213 across Aug 11→12). The Aug-10 log shows the 928162c retry firing in
     production and saving that night's run. Streak Aug 9-12 green; the 06-08 gap is
     visible as missing dirs, exactly as designed.
  4. THE CCL ANSWER, and the gap it exposed. User held CCL through 6 BUY signals in 8
     days (incl. one at 30.14, the worst entry; June's 4 resolved CCL signals went -11.0,
     -7.8, -3.0, +0.2 pct). The system measures fixed-horizon population alpha ONLY — no
     position awareness, no exit rule, no path (MAE/MFE parked), no memory of repeat
     signals. That is not a bug; it is a missing layer, and it is the only pre-December
     user-facing value identified all week. BUILD STARTED Aug 13: a position/exit layer
     that READS signals and never creates them — own positions.db, read-only against
     tracker.db, R1 untouched by construction.

STILL OPEN: the position layer build (see next entry); third off-VM setup_log copy
  (REVIEW 2026-08-17); CLAUDE.md trim stages 3-4 (REVIEW 2026-08-17).

===========================================================================================
  (commit follows)  Aug 13 — positions layer BUILT + DEPLOYED, live-verified end to end

WHAT SHIPPED: a 9th tab, "הפוזיציות שלי" — the layer the CCL case showed was missing.
  User enters symbol/entry/date/optional stop; the tab shows live price, net P&L
  (0.08%/side commission, both sides), NYSE trading-days held, and two alerts:
  STOP (gross <= -stop_pct; the user's own "first entry going wrong = exit signal"
  rule) and HORIZON (>= 10 trading days — past the system's measured horizon, where
  it has no claim). Next to them: the tracker's own recent signal count on the
  symbol + how its past resolved signals ended — the exact info whose absence let
  6 fresh CCL signals in 8 days read as 6 independent opportunities.

R1 SAFETY IS STRUCTURAL, NOT POLICY: own positions.db (WAL, in backup set as of
  this deploy — backup_dbs.py DATABASES + the Mac pull picks it up automatically);
  tracker.db is opened mode=ro inside db.py, so this layer physically cannot write
  the research DB; a unit test locks the mode=ro refusal. No scanner, verdict, or
  logging path was touched. setup_log/tracker schemas untouched.

SURFACES: db.py (positions CRUD + 3 pure functions: net pnl, trading-days-held,
  alerts), models.py (2 pydantic v2 request models), api.py (GET/POST /api/positions,
  POST /api/positions/{id}/close, 60s batched price cache; registered ABOVE the
  static mount — a "/" mount swallows routes registered after it), App.jsx
  (PositionsView), test_scanners.py (+13 tests, gate now 87), backup_dbs.py (+1 db).

ONE LIVE BUG CAUGHT AND FIXED DURING DEPLOY: yf.download(group_by="ticker") returns
  MultiIndex columns even for ONE symbol; the len(symbols)>1 extraction branch
  silently priced nothing for a single open position (current_price null on the
  first production GET). Fixed to probe-per-ticker-then-flat; second deploy verified
  live: CCL 27.67, net -3.88%, STOP firing at -3.7% <= -3.0%. Smoke row deleted;
  positions.db live and EMPTY. NOTE for future debugging: ambient Yahoo "Invalid
  Crumb" 401s exist in the journal (4 yesterday, pre-deploy) — a null current_price
  can also be Yahoo flaking, which the tab renders as '—' with price alerts skipped,
  never an error.

  FOLLOW-UP same day (d9d8ed4): the positions symbol cell now follows the site-wide
  convention — Yahoo Finance link + ExternalLink icon, plus the small yellow TV badge,
  matching Volume Leaders and the scanner. Cosmetic only, no logic touched.

  VERIFIED Aug 14 08:17: positions.db entered the backup set on its first run after
  the deploy — 4 files in the pull, all PRAGMA integrity_check ok, heartbeat lists all
  four. The layer is in real use (2 open positions logged by the user on Aug 13).

  EDIT ADDED Aug 14 (user asked — create and close existed, update did not):
  PATCH /api/positions/{id} + a pencil icon that turns the row into inline
  inputs (entry price, date, stop, notes) with ✓/✗. The load-bearing detail is
  partial-update semantics: the endpoint reads pydantic's model_fields_set, so
  an ABSENT key leaves a field alone while an EXPLICIT null clears it —
  conflating the two is how an edit form silently wipes a stop the user still
  wanted. db.position_update_row refuses non-editable fields (status,
  exit_price) and refuses closed positions outright. 9 new tests, gate now 96.
  Live-verified on a throwaway row: add stop 3% + change price, then null-clear
  the stop leaving price untouched. The user's two real positions were not
  touched — both still carry NO stop, so only HORIZON can fire on them.

  SHARES + DOLLAR P&L Aug 14 (user asked — percent alone is abstract):
  nullable `shares REAL` (idempotent ADD COLUMN; the 3 live rows predate it and
  keep NULL), plus cost_basis, net_pnl_usd, and a portfolio roll-up in the GET.
  DESIGN CALL: the dollar figure is DERIVED from the percentage rather than
  charging 0.08% against each leg's actual value. The per-leg model is a hair
  more exact (<0.01pp on a 50% move, far under the spread actually paid), but
  the two numbers sit side by side on screen and two subtly different commission
  models shown together is the worse bug. One formula, guaranteed consistent.
  NULL shares never guesses 1 — every dollar field returns None, and the totals
  row reports `skipped` so a partial total can't read as complete. 13 tests
  (gate 109) incl. an idempotent-migration test, since the live DB has rows.
  Live-verified: F @10.00 x100 = $1,000 basis, +41.04% -> +$410.40, roll-up
  agrees, 3 real rows skipped for lack of shares. Migration preserved all three
  (CCL/PLTR/META, snapshot at /tmp/positions_pre_migration.db on the VM).
  ONE TEST BUG WORTH REMEMBERING: I first asserted -$360 where the answer is
  -$103.46 — multiplied the percentage by share count instead of by basis. The
  consistency assertion (usd == basis*pct/100) passed and caught it.

  ROUNDING BUG, found by the user reading his own screen (Aug 14): CCL showed
  a $20.66 loss where the arithmetic gives $20.54. Cause: position_net_pnl_usd
  multiplied the basis by the percentage AFTER it had been rounded to 2dp, so
  the rounding error scaled with position size — $0.12 on a $3k position, $0.60
  on the $63k one. Fixed by extracting _position_net_pct_raw() and rounding only
  at the edges; pct and usd now both derive from the same unrounded value.
  Regression test pins both live cases. LESSON: when one displayed number is
  derived from another, round once, at the very end — never chain roundings.

  UI REWORK Aug 16 — user: "the interface is very bad". First item: clicking the
  pencil should open the row in the wide form at the top, not sprout tiny inputs
  inside narrow table cells. Correct, and it was a bad call on my part: the
  cells are ~80px wide in an RTL table, so a date + number + text input in them
  were unusable, and the caret could land off-screen.
  Now ONE form serves both modes (editingId null = open, an id = edit): the
  pencil fills it, scrollIntoView brings it into view, the form and the row both
  highlight, symbol is disabled while editing (it is not an editable field), and
  a "cancel edit" escape sits in the header. Fields got real <label>s — the
  placeholders were truncating ("% סטופ (רשות)" rendered as "פ (רשות)%").
  Also: an inline hint when editing a position with no stop, since that is
  exactly the state that makes a STOP alert unreachable.
  CAUGHT PRE-DEPLOY: useRef was used but never imported. The Vite build passed
  clean — it does not resolve identifiers — so this would have been a runtime
  crash on the tab. A grep audit of every identifier the new code touches is now
  part of the routine; a green frontend build is NOT a correctness signal.

  PRIVACY HOLE CLOSED Aug 16 — user: "this is a site any user can see; if I
  entered my positions, anyone can see them". He was right about the design and
  I had shipped it. Facts as found: BASIC_AUTH_USERS held exactly ONE account
  (elim), so nothing was actually exposed — but the positions table had no owner
  concept at all, so a second entry in that env var would have shown every user
  the whole book. Worse, ids are sequential AUTOINCREMENT, so PATCH/close by a
  guessed integer would have hit another user's row.
  FIX: `owner TEXT` + index; owner is a REQUIRED positional arg on
  positions_list / position_open_row / position_update_row / position_close_row,
  so a call site that forgets it raises TypeError instead of silently returning
  everything. Owner is part of the WHERE clause, not a check after the lookup —
  another user's id is simply "not found", leaking nothing about existence.
  _require_auth already returned the username; it was declared app-wide only, so
  each endpoint now also takes `user: str = Depends(_require_auth)` — that is
  what actually hands the identity to the handler.
  BACKFILL POLICY (fail closed): api.py claims ownerless rows for the sole
  account ONLY when exactly one is configured. With 2+ accounts the rows stay
  owner=NULL and are invisible to EVERY reader — an unowned position must never
  default to shared. Verified live: "backfilled owner on 3 pre-ownership row(s)",
  all three now owner=elim, the tab still shows them, wrong password and unknown
  user both 401. Snapshot before the migration: /tmp/positions_pre_owner.db.
  10 isolation tests (gate 120) cover read scoping, the guessed-id edit/close
  attack, per-user totals, and that owner is required rather than defaultable.

  manage_users.sh Aug 17 — user reported the nano recipe "doesn't let me change
  the password". Server was fine (nano/vim/vi/ed all present, file 664 and owned
  by elimaoz99, no immutable flag), so the cause was almost certainly a missing
  TTY when three lines are pasted at once; `ssh -t ... 'nano ~/…/api_data.env'`
  is the one-liner fix. But hand-editing a secrets file is the wrong interface
  anyway: one stray comma makes BASIC_AUTH_USERS parse to nothing, and the
  fail-closed design then 503s every request — locked out with the site down.
  The script does list / passwd / add / remove with: password never on the
  command line (read -s, piped on stdin — argv is world-readable via ps),
  timestamped backup, parse-back check before writing, automatic restore if the
  rewrite fails, service restart plus an is-active verdict, and three refusals —
  empty password, removing the LAST login, and removing the FIRST login (both
  live_tracker.py and warm_volume_cache.sh authenticate as pair #1, so removing
  or reordering it silently changes which account the crons run as). add always
  appends last; passwd rewrites in place; both preserve position.
  BUG CAUGHT IN REHEARSAL, not in production: the helper was `python3 - <<'PY'`,
  but a heredoc IS stdin, so the program landed where the password should have
  been and every password silently became EMPTY — the rewritten line read
  `elim:,dani:`, which would have locked the account out on the next restart.
  Fixed by writing the helper to a temp file; an explicit empty-password refusal
  now backstops it. LESSON: rehearse destructive scripts against a throwaway
  copy before they ever touch the real file — this one would have cost the login.

  I LOCKED THE USER OUT WITH MY OWN SCRIPT — Aug 17, ~40 minutes.
  He ran `manage_users.sh passwd elim` exactly as instructed and typed a Hebrew
  password. The script accepted it: I validated length, ',' and ':' but never
  ASCII. HTTP Basic Auth carries credentials as base64 of raw bytes with no
  agreed encoding, so the browser encodes one way and Starlette decodes another
  and the compare can never match. Result: he could not log in, AND
  warm_volume_cache.sh started failing (6x 401 in 40 minutes) because it
  authenticates with the same pair. live_tracker.py at 20:05 would have been
  next — that is the R1 feed.
  DIAGNOSIS THAT WORKED, and the shape to reuse: never print the secret. A
  python one-liner reported `user='elim' password_length=8 ascii_only=False`
  and then a Counter of unicodedata categories gave `{'HEBREW': 5, 'ascii': 3}`.
  Enough to identify the bug exactly, with the password never on screen or in a
  log. The same check now runs as a post-change verification, building the
  Authorization header the way live_tracker._basic_auth_header does — a 200
  there is a real statement about the crons, not a guess.
  FIX: ASCII-only guard in TWO layers (bash grep -P '[^\x20-\x7E]' plus a python
  backstop), rehearsed against a Hebrew password of the same 5-heb/3-ascii shape
  before deploying. No data was lost at any point.
  TWO LESSONS: (1) a validator that checks length and delimiters but not
  CHARACTER SET is not a validator for anything that crosses a protocol
  boundary; (2) both of this script's real bugs — the heredoc-stdin one and this
  one — were caught by rehearsing on a throwaway copy, and the one I did NOT
  rehearse in place (the live passwd run) is the one that bit. Rehearse the
  actual invocation, not just the code path.

  VM REBOOT Aug 17 (unrelated, done same session): kernel 6.17.0-1020 -> 1022
  plus libc6, pending since the Jul 5 downgrade (uptime 6w1d). elimaoz99 has NO
  sudo for reboot (NOPASSWD covers only systemctl restart stock-app.service,
  chown, chattr) and gcloud is not installed on the Mac, so it is a Console
  Stop -> Start — NOT Reset, which is a power cut against SQLite in WAL mode.
  Timing rule worth keeping: reboot outside 16:30-23:00 IDT, because that window
  is when the */25 warm cron collects setup_log rows and a gap there is a
  sample-composition gap, not just downtime.
  Verified after: kernel 1022, no reboot-required, service active/enabled with
  NRestarts=0, auth 200 on health+positions+volume-leaders, 12 crontab lines,
  5/5 DBs pass integrity_check, setup_log 1777 / signals 234 / outcomes 151,
  all three positions intact, disk 29%, mem 657MB of 3910 (the Jul-5 no-swap
  decision still holds).

  SIGNALS COLUMN REWRITTEN Aug 17 — user: "the signals thing isn't clear to me".
  It rendered as "0 אותות/14י" and, worse, on CCL the BiDi algorithm tore the
  leading "6" off its Hebrew word and parked it at the far edge of the cell:
  "אותות/14י · ממוצע -5.4% 6". Two separate defects — an unexplained format and
  a bidirectional-text bug — in a column whose whole job is context.
  Now two labelled lines, every number wrapped in <bdi> so the isolate keeps it
  with its word: "6 אותות ב-14 ימים" / "קודמים: 4 · ממוצע -5.4%", with an
  explicit empty state ("אין אותות לאחרונה") instead of a bare 0. The count of
  resolved rows is shown INLINE next to the mean — a mean with no n was the
  misleading part, since it is at most 5 signals on one symbol. Header carries a
  dotted underline and a tooltip saying what the column is, that it is about the
  SYMBOL and not the user's position, and that a single-symbol sample is too
  small to conclude from; the row tooltip lists each past signal and repeats
  "anecdote, not evidence".
  RULE: in this RTL UI, any number adjacent to Hebrew text goes in <bdi>. The
  existing BiDi guidance covered code and filenames on their own lines; inline
  numerics need the same care and are easier to miss because they only break
  once the value has more than one digit.

  ────────────────────────────────────────────────────────────────────────────
  RVOL SPLIT INTO TWO COLUMNS — Aug 19 2026 (Reversion Hunter, display only)

  ORIGIN: user asked which name on the Reversion Hunter table looked readiest to
  bounce. NUE showed RVOL 15x; I ranked it first largely on that, calling it
  capitulation volume. User then opened the TradingView chart, whose sidebar read
  Volume 3.05M vs Average(30D) 1.36M — 2.2x, not 15x. I over-corrected and said
  the screener number was simply wrong. It was not.

  WHAT IT ACTUALLY IS: get_tod_rvol_cached() and its fallback both divide ONE
  5-minute bar by the same 5-minute slot on prior days. Reproduced NUE live off
  the server that afternoon:
        14:45 -> 22.7   14:50 -> 23.8   14:55 -> 14.8
        15:00 -> 12.3   15:05 ->  3.3   15:10 ->  4.9
  So 15 was a real, correct number for a burst — and the displayed value is just
  whichever bar happened to be last when the 900s cache was built. A metric that
  moves 23.8 -> 3.3 in fifteen minutes cannot be used to rank rows against each
  other, which is exactly what I had done with it.

  SHIPPED: scanners.compute_day_rvol() + rvol_day / rvol_day_basis on the
  reversion payload; frontend gets a "נפח יומי" column next to the renamed
  "פרץ 5ד". Numerator and both baselines already arrive in the Yahoo screener
  quote, so zero extra network calls on the e2-medium. 9 unit tests including a
  regression pinning the real NUE figures (3,420,518 / 1,534,072 = 2.2).
  Basis prefers 3-month over 10-day: on NUE the two gave 2.2 vs 2.9, and a
  10-day window is contaminated for any name that has been selling off for a
  week — i.e. the whole day_losers population. Basis is returned and shown in
  the tooltip rather than left implicit.

  TWO DEFECTS FOUND, BOTH DELIBERATELY NOT FIXED:
  1. intraday_cache.db holds only the most-actives universe — 166 symbols. Of 10
     names off that evening's day_losers table, 8 had ZERO rows (NUE, ZTO, KEYS,
     TTMI, WDC, LRCX, CRWD, DELL); only NBIS and AAOI were covered. So this tab
     nearly always takes the mean-over-10-days fallback, and the documented
     "MEDIAN not mean" rule is not in force here. Left alone on purpose: that
     same path feeds the VL RVOL-slope gate whose attribution clock restarted
     Aug 5, so mean->median is a measurement-regime change for the next bundle,
     not an incidental tweak.
  2. rvol_alert threshold 5.0 has never been calibrated. On a per-bar metric >5
     is routine — 176/1362 gainers and 79/583 VL logged rows clear it, all-time
     max 264.4 (CBZ). And the flag is set only in the reversion path, which has
     logged zero setup_log rows ever, so it has never been measured at all.

  DEPLOY HELD until after 16:00 ET. The change is display-only and had no
  urgency, and a restart at 15:38 ET would have wiped _rvol_history mid Power
  Hour — blinding the RVOL-slope gate for the rest of an instrumented session
  to ship a column rename. Committed first, deployed after the close.

  LESSON: the misread was mine before it was the UI's. A column labelled RVOL in
  a table of daily movers reads as daily by default, and I never checked which
  question it answered before ranking on it. When a displayed number drives a
  ranking, verify its denominator — and when the user's own chart disagrees with
  the app, the app is not automatically the one that is wrong.

===========================================================================================
  (commit follows)  Aug 23 — banks tab: the curve it models vs the curve that exists today

CONTEXT: the "עקום התשואות ← בנקים" tab shipped Aug 20 (commit eeef4f1). Its design,
  guardrails and findings live in CLAUDE.md §"Yield Curve → Banks"; this entry is the
  first handoff note on it, and it is about a labelling bug, not the model.

THE REPORT (Aug 21): user, looking at the live tab — "זאת לא התשואה האמריקאית".
  Displayed: 3m 3.73% / 2y 3.97% / 10y 4.42%. Actual that day: 3.86 / 4.19 / 4.65.

IT WAS AMERICAN. The series are DGS3MO/DGS2/DGS10 — US Treasuries. What they were NOT
  is current: fetch_curve() pulls frequency=q&aggregation_method=avg, so the panel was
  showing the average of the last CLOSED quarter, Q2 2026 (Apr-Jun). The header above it
  rendered data.updated_at — the CACHE REFRESH time, i.e. that morning's date. A
  three-month-old average under today's date reads as a live quote that is simply wrong.

WHY THE QUARTERLY AVERAGE STAYS: Alessandri & Nelson is estimated on quarterly data.
  Every coefficient, impulse response and scenario in the module is defined against
  quarterly observations. Feeding it daily values would leave the numbers looking fresher
  and meaning nothing. The fix could not be "make it live" — it had to be "say which one
  this is". User picked the label+live option over label-only.

SHIPPED:
  bank_rates.py — _fred_latest() (sort_order=desc with a lookback window, because FRED
    stamps non-trading days ".") and fetch_curve_live(), returning curve.live: the latest
    DAILY observation of the same three series, each value carrying its own as_of so a
    lagging series is never hidden behind a shared date. Sequential with time.sleep(0.5),
    the same FRED rate-limit rule as fetch_curve (the Jun 1 429-cached-as-nulls incident).
    Wrapped try/except -> None: a failure here costs the live row and nothing else.
  App.jsx — the five stat cards are now captioned "קלט המודל · ממוצע רבעוני Qn YYYY";
    BRLiveQuote renders under them, labelled "לא נכנס למודל", and returns null if
    curve.live is absent. Header date prefixed "עודכן" so it reads as freshness, not as
    the data's date. Chart caption shows "Q2 2026" instead of the raw "2026-04".
  _SCHEMA 2 -> 3, so every schema-2 disk cache is discarded rather than served for 6h
    with curve.live silently missing — the same rule the resilience layer added.

BOUNDARY HELD: curve.live is display-only, in the same class as the sector heatmap and
  the leveraged sentiment strip. It is not a model input, not a covariate, not a gate,
  writes nothing to setup_log, and is outside R1 and Stage B entirely. estimate(),
  impulse_response() and the scenario endpoint still read ONLY the quarterly averages —
  none of those call paths was touched.

VERIFIED: 51 test_bank_rates tests pass, check_guardrails 77/77 IRON present, vite build
  clean, deploy.sh green end to end (bank_rates.py is already in BACKEND_FILES). On the
  server after deploy: schema 3, 17/17 banks still estimating, quarterly 3.73/3.97/4.42
  next to live 3.87/4.19/4.69 as of Aug 20. Server cache warmed by hand post-deploy so
  the first visitor did not pay the 17-call FDIC cold fetch.

THE GAP CLOSES ITSELF Oct 1, when Q3 closes and Jul-Sep enters the model. Until then the
  two rows will visibly disagree, which is now the intended reading rather than a defect.

LESSON: same shape as the RVOL misread four days earlier — a number was correct and its
  label was not, and the label is what everyone actually reads. Both times the user's own
  reference disagreed with the app and the user was right about the reading even though
  the data was right too. When a panel shows a quantity that has an as-of date, the as-of
  date belongs next to the quantity, never in a header that means something else.


================================================================================
AUG 23 2026 — COST REDUCTION + LOCAL-FIRST MOVE (₪88.70 -> ₪44/mo)
================================================================================

WHY: user saw the GCP bill "getting high" and a scary "+47%" in the console.

THE +47% WAS AN ARTIFACT — the bill was never rising. The console compared an August
full-month forecast against a July that only had 22 billed days (Jul 1-9 cost ~₪0, a
credit ran out around Jul 10). Real run rate: ₪2.80/day Aug 1-22 vs ₪2.74/day Jul 10-31
= +2%, i.e. flat. Always compare like-for-like periods before reacting to that banner.

COMPOSITION (Compute Engine was 100% of the bill; the 2nd project charges ₪0):
  vCPU+RAM ~79% | 30 GB disk ~10% | external IPv4 ~10% | egress ₪0
  Egress measured directly: 2.80 GB in 5d18h = ~14.6 GB/mo, far under the 200 GB free tier.
  NOTE: E2 gets NO sustained-use discount (E2 is absent from the SUD-eligible list:
  N1/N2/N2D/C2/M1/M2). An early analysis wrongly credited the "savings" line to SUD.

WHAT WAS DONE (in this order — the order matters):
  1. Reserved the external IP as static BEFORE stopping anything (7 files hardcode it).
  2. Added a 4 GB /swapfile + fstab entry. THIS MUST COME BEFORE THE DOWNSIZE.
  3. Capped the journal: SystemMaxUse=200M, freed 1.7 GB (it had grown to 1.9 GB).
  4. Stopped the VM -> machine type e2-small -> started. Verified: IP unchanged, swap
     auto-mounted, service active, all 11 crons survived, authenticated API 200s.
  5. Stress-tested 2,900 MB: survived, 0 OOM, swap used only 220 MB.
  6. Installed Tailscale; phone joined and reached the app over the tailnet.

WHY NOT e2-small EARLIER / WHY THE SWAP IS NON-NEGOTIABLE: average utilisation looks like
the box is wildly overprovisioned (2.4% CPU, 18% RAM, load 0.01) — and that is a trap.
`sar` history showed 2026-08-19 05:30 hitting 2,904,716 kB (2.84 GB, 72.5%) during the
05:00 pre_scan.py cron. Normal pre_scan peak is ~0.9 GB; that day was 3x. CPU also touches
100% on every retained day. Without swap, e2-small would have been OOM-killed mid-scan.

WHAT WAS DELIBERATELY *NOT* DONE:
  - No 1-year CUD. It was recommended, then withdrawn: a 12-month lock is wrong when the
    horizon in question is ~7 weeks. (For the record CUDs DO cover e2-medium, and under a
    commitment contract e2-medium counts as 1.0 core, not 2 vCPUs — size it accordingly.)
  - No GCE instance schedules / Cloud Run / GitHub Actions. GH Actions in particular is
    DISQUALIFIED for live_tracker: scheduled workflows are documented as not guaranteed and
    routinely run 15-60 min late, and R1's endpoint is measured from entry_price at signal
    time, so jitter corrupts the very quantity under test.
  - Ports 80/443 were NOT closed. See the domain note below.

R1 TIMELINE WAS RECOMPUTED AND IS MUCH SOONER THAN "DECEMBER":
  Counting by RESOLVED rows is a lagging measure. By LOGGED rows: 45 of 60 already logged
  (8 pending resolution), 23 of the 25 required distinct days already banked. Recent rate
  ~5/week. That puts the gates at roughly MID-OCTOBER 2026, not December.
  All of this was counted BLIND — verdict/confidence/date columns only, no outcome column
  was ever read, and r1_sitting.py --unblind was NOT run.
  Stage B (lev_spec) SEPARATELY HIT ITS min_n: the BREAKOUT CONFIRMED pool reached
  exactly 50/50 on 2026-08-21. That analysis is unblockable now and costs nothing.

THE DOMAIN — stock-predictor.online:
  Registrar NameCheap. Registered 2026-04-17, EXPIRES 2027-04-17. Paid annually, already
  paid, NOT a monthly cost, and NOT billed through GCP. nginx (active) terminates TLS via
  Certbot/Let's Encrypt and proxies to 127.0.0.1:8000.
  IMPORTANT CORRECTION: port 8000 is ALREADY firewalled off from the internet (connections
  time out). The site is public over 80/443 THROUGH NGINX. Anyone planning to "take the
  site off the internet" must close 80/443 — closing 8000 would accomplish nothing.
  Left open on purpose: no cost pressure until Apr 2027, and closing 443 is the easiest way
  to lock yourself out. Revisit after R1 closes.

LOCAL-FIRST WORKFLOW (new, working):
  ~/Desktop/Stock-predictor/run_local.sh          syncs, then serves on localhost:8010
  ~/Desktop/Stock-predictor/pull_caches_local.sh  ONE-WAY server->Mac sync of 7 DBs (~20 MB)
  Verified 200 on /, /api/health, /api/gainers, /api/reversion-leaders, /api/positions,
  /api/falling-knife-stats, /api/macro-score, /api/volume-leaders — against synced data,
  with live yfinance price lookups working.
  GOTCHAS FOUND WHILE SETTING THIS UP:
    - requirements.txt IS INCOMPLETE. Also needed: cachetools, python-dotenv, requests,
      httpx, scipy, anthropic, telethon. Without them `import api` raises ModuleNotFoundError.
      Worth fixing in git.
    - api_data.env did not exist on the Mac; pulled from the server, chmod 600. api.py
      self-loads it for FRED_API_KEY and BASIC_AUTH_USERS.
    - THE SERVER HAS NO sqlite3 CLI. Use venv/bin/python3 for any DB work there;
      pull_caches_local.sh uses python3 sqlite3.backup() for consistent online copies.
    - pull_caches_local.sh has NO push path by design — tracker.db pushed Mac->server
      destroyed server data on Jun 14 2026.
  DO NOT widen pull_backup.py to carry these caches. Its DATABASES list is deliberately
  only the 4 unrebuildable research DBs (~350 KB/day, 365-day rotation); adding 20 MB/day
  of rebuildable cache breaks that design and the wake-race hardening around it.

STILL OPEN:
  - Verify the first e2-small runs of the 03:00 / 05:00 / 20:05 crons (not yet observed).
  - Tailscale on the Mac (phone done; Mac optional — run_local.sh needs no tailnet).
  - Namecheap: check whether auto-renew is on, well before Apr 2027.
